{
  "name": "@side-quest/vault-git-transaction-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Facade-backed transaction boundary for configured Super-vault writes.",
  "sideQuest": {
    "sourceLinkedBin": true
  },
  "bin": {
    "vault-git": "./src/cli.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "vault-git": "bun run src/cli.ts",
    "test": "bun test tests",
    "test:smoke": "bun test tests/smoke",
    "test:smoke:pr": "bun test tests/smoke/main-ahead.integration.test.ts tests/smoke/stale-writer.integration.test.ts tests/smoke/crash-resume.integration.test.ts && bun test tests/smoke/durable-phase-matrix.integration.test.ts -t 'intent_durable|leased|checking|committing|push_pending|repairable|human_required|blocked|closed'",
    "typecheck": "tsc --ignoreConfig --noEmit --strict --target ESNext --lib ESNext --module Preserve --moduleDetection force --moduleResolution bundler --allowImportingTsExtensions --verbatimModuleSyntax --skipLibCheck --noFallthroughCasesInSwitch --noImplicitOverride --resolveJsonModule --types bun,node src/*.ts tests/*.ts tests/smoke/*.ts"
  },
  "dependencies": {
    "@side-quest/cli-command-facade": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
