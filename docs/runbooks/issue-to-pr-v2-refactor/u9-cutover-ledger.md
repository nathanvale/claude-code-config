# U9 regression probes + public cutover - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 1 | stale-v1-path-in-u7-hot-router | closed | low | `runbooks/issue-to-pr-v2/issue-to-pr.md` L16 still narrates "v1 hot file ... stays available as a reference until U9 flips the public entry point", which becomes literally inaccurate the moment U9 lands. The hot router is read-only inside U9. | Closed: `deferred-to-u7-followon` per the U8 runbook's closing reasons — U7 owns the hot file. Surfacing here so the U7 follow-on backlog records it. |
| 2 | stale-v1-path-in-loop-fallback-example | closed | low | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` L300-310 hosts a `/loop` fallback example whose runbook path is the v1 install (`~/.claude/runbooks/issue-to-pr/issue-to-pr.md`). The parenthetical at L308-310 already acknowledges this and points at U7. references/* are read-only inside U9. | Closed: `deferred-to-u2-followon` (or U7 follow-on, since U7 owns the path narrative the example must mirror). |
| 3 | stale-v1-path-in-stage-6-ship-example | closed | low | `runbooks/issue-to-pr-v2/references/stage-6-ship.md` L37-43 uses `~/.claude/runbooks/issue-to-pr/` as the example install root in the MCP runner / shell fallback paragraph. After cutover the example should generalize to `~/.claude/runbooks/` or name the v2 path. references/* are read-only inside U9. | Closed: `deferred-to-u2-followon` — U2 owns the per-stage references. |
| 4 | v2-readme-h1-still-says-shadow-install | fixed | low | v2 README H1 still said "(v2 shadow install — human index)" after U9 trimmed the body away from shadow language, creating an H1/body inconsistency. Reviewer flagged as out-of-scope per anti-list ("no v2 README restructure beyond the one-paragraph caveat trim"); on second look, a 1-word H1 swap is consistent with the spirit of U9 and avoids a known-stale title surviving cutover. | Trimmed H1 from "(v2 shadow install — human index)" to "(v2 install — human index)". No structural change. |
| 5 | probe-startup-route-anchors-cite-next-and-contract-not-state | fixed | low | The `probe-startup-route` row's `probe` field was `cli.ts state --json`, but the cited test anchors covered `next` (no-imperative) and `contract` (route_ids catalog) commands too — three of the five anchors test commands other than `state`. The startup route protocol legitimately reads all three commands, so broadening the probe field is more honest than narrowing the anchor list. | Updated the `probe` field to `cli.ts state --json, cli.ts next --json, cli.ts contract route_ids --json` so the probe target matches what the anchors actually cover. |
| 6 | probe-runbook-version-mismatch-omits-continuation-evidence-cli-anchor | fixed | low | The `probe-runbook-version-mismatch` row's v2 scenario names `continuation-evidence-present` as one of four version-skew states, but the cli.test.ts anchor list omitted the test that exercises this fourth state at the CLI envelope layer (cli.test.ts L1367 "continuation-evidence-present clears the stop-required gate and lets routing proceed"). The route.test.ts equivalent was already cited; this was an envelope-layer citation completeness gap. | Added the cli.test.ts L1367 anchor verbatim. |

