# U10 CLI smoke matrix - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 1 | false-alarm-process-exit-divergence | closed | low | The U10 seam runbook initially named "process exit code vs envelope exit_code divergence" as the central drift-residual U10 would pin (based on a U9 manual-smoke `exit=$?` reading suggesting the process exited 0 even on usage errors). Writing Block 10's smoke tests disproved the premise: process exit codes today already match envelope `exit_code` for success (0), usage errors (64), validation errors (1), and unexpected errors (70). The U9 reading was a shell artifact — a piped `head -3` swallowed the real `$?`. | Closed: false alarm. Block 10 inverted from "pin the divergence as-is" to "pin the alignment as a regression surface". Seam runbook and smoke file comments updated to reflect ground truth. The "file as separate agentic-CLI design issue" task is deleted from the U10 plan. |
| 2 | block-3-plan-route-fixture-requires-real-digest | closed | low | The smoke included a Block 3 test asserting "AC-confirmed but no-plan ledger reports route_id: plan", but the confirmation-state derivation in `lib/ledger.ts` requires a real `ac_digest` in frontmatter matching the computed digest of the ledger's `## Acceptance criteria` content. A static fixture cannot satisfy this without computing the digest at test time — fragile for U10 purposes (smoke is process-boundary, not digest-mechanics). | Closed: out-of-u10-scope. Removed the failing test and the `ledgerConfirmedNoPlan` fixture. The remaining Block 3 tests (no-ledger, pending-AC, blocked-frontmatter, mismatched-version-skew) cover the route classifier surface adequately without needing a "plan" route fixture. Computing real digests at test-fixture time is a separate maintainability concern; if it becomes important the v1-style `runDecompose` digest helper can be lifted into a smoke fixture helper. |
| 3 | smoke-hardcoded-command-and-role-lists | fixed | low | Block 2 hardcoded `commandsRequiringJson = ['state', 'next', 'contract', 'diagnose', 'packet']` and Block 7 hardcoded `['builder', 'proposer', 'validator', 'patch-proposal']` as the iteration target for the required-flag matrix. The U10 implementation pattern (runbook line 269-273) explicitly forbids this: hardcoded catalog lists silently skip new entries when the runtime catalog drifts, defeating the central design rule of reading HELP_DATA at test time. | Fixed: Block 2 now drives iteration from `helpCommands().map(c => c.name)`. Block 7 derives `rolesWithRequiredFlags = helpPacketRoles().filter(r => helpPacketRoleFlags(r).required.length > 0)`. A new role or command added to HELP_DATA now gains smoke coverage automatically. |
| 4 | smoke-missing-response-shape-and-catalog-pins | fixed | low | The first review pass found multiple partial pins of the documented `HELP_DATA` response shapes: `state_response_shape` only pinned 7/17 fields (missing `runbook_version`, `runbook_version_skew`, `ledger_path`, `ledger_exists`, `frontmatter_status`); `diagnose_response_shape` similarly omitted 4 documented fields; `contract_slice_response_shape.ordering` discriminator was not pinned; `confirmation_state` only pinned `acceptance_criteria` without `batch_contract` and `digests`; the `error_codes` catalog had no membership-completeness assertion. | Fixed: Block 3's state-shape test now pins the full documented field set including `runbook_version`, `runbook_version_skew`, `ledger_path`, `ledger_exists`, `frontmatter_status`. Block 6's diagnose-shape test pins the U6 additive fields. Block 5 asserts `ordering` is `sorted` or `catalog` for every slice. Block 3 pending-AC test pins all three `confirmation_state` sub-keys. Block 8 gains an explicit catalog-membership assertion enumerating the 10 documented error codes. |
| 5 | smoke-error-envelope-shape-not-cross-checked-with-catalog | fixed | low | `assertErrorEnvelopeShape` only validated types on `exit_code`, `severity`, `recoverability`, `retryable`, and `hint`. A future runtime drift where the envelope emits the wrong `exit_code` (e.g. 1 instead of 64), wrong `severity`, or wrong `hint.action` for a given error code would not surface. The documented catalog values in `HELP_DATA.error_codes` were not cross-referenced. | Fixed: `assertErrorEnvelopeShape` now looks up the catalog entry via `findErrorCodeEntry(expectedCode)` and asserts the envelope's `exit_code`, `severity`, `recoverability`, `retryable`, and `hint.action` match the catalogued values exactly. A drift between runtime envelope and documented catalog now fails the matching Block 8 test deterministically. |
| 6 | block-9-stdout-stderr-separation-only-covered-default-mode | fixed | low | Block 9's stderr-empty assertions only exercised the default-mode happy path (success + `unknown-command`). The documented `envelope_streams.stderr` contract (HELP_DATA: "Zero-or-more JSON Lines diagnostic records when --verbose or --debug is passed") and the cross-stream contamination invariant (no envelope on stderr, no diagnostic on stdout) were not pinned. | Fixed: Block 9 gains two new tests. (a) Error-level diagnostic separation: `state` against a no-frontmatter ledger triggers `command.fail` at error rank 3, which lands on stderr by default — pins that the stderr lines parse as JSON Lines, have `event`/`level` fields, and never carry envelope fields (`status`, `schema_version`). (b) `--verbose` lowers the threshold to info rank 1, so `command.dispatch` lands on stderr; pins that stdout is still single-envelope and stderr is JSON Lines without envelope shape. |
| 7 | block-8-ledger-validation-failed-trigger-was-conditional | fixed | low | The original Block 8 test for `ledger-validation-failed` used `if (status === 'error') assertErrorEnvelopeShape(...) else throw new Error(...)` because the malformed-YAML fixture (broken Batches body) relied on parser-internal tolerance heuristics in `parseLedgerBatchRows`. If the parser became more permissive, the test would still throw, but with a confusing message that looked like a fixture bug rather than a contract change. | Fixed: replaced with `ledgerNoFrontmatter()` — a ledger with no `---` delimiter at all. The frontmatter reader's invariant (`ledger has no frontmatter block`) is contract-stable and deterministically raises a DecomposeError that maps to `ledger-validation-failed` with `exit_code: 1`. Block 8 now uses a single explicit assertion instead of conditional branching. Block 10 also gains a `ledger-validation-failed → exit 1` alignment test using the same fixture. |
| 8 | smoke-fixture-builders-duplicate-scaffolding | fixed | low | Four ledger fixture builders (`ledgerPendingAc`, `ledgerBlockedFrontmatter`, `ledgerVersionSkewMismatched`, `ledgerMalformedYaml`) repeated ~25 lines of identical scaffolding (`# Issue 1`, AC, Batches, Findings data, Findings table). Only the frontmatter differed across three of them. The runbook (line 274-276) explicitly endorses "string templates with substitution" for ledger fixtures. | Fixed: collapsed to a single `buildLedger({ frontmatter, batchesYaml? })` helper. Per-fixture intent (which frontmatter fields each scenario exercises) is now visible at a glance instead of buried in copy-paste boilerplate. Cut ~80 lines of duplication. |
| 9 | runcli-no-timeout-no-kill-on-hang | fixed | low | `runCli` had no proc.kill() in a finally block. If `cli.ts` ever hung (e.g. future blocking I/O), `bun test`'s per-test timeout would fail the test but the subprocess would leak. The Promise.all also had no cleanup on rejection. | Fixed: `runCli` now wraps Promise.all in try/finally and calls `proc.kill()` when `proc.exitCode === null` (i.e. the subprocess hasn't exited cleanly). No-op for the happy path; bounded protection against future hangs. `beforeAll` error message expanded with `exitCode` and longer stdout/stderr context for diagnosability. HELP_DATA accessors now assert the cache is populated, so a beforeAll failure surfaces a clean message instead of a confusing "role X not in catalog" downstream. |
| 10 | block-10-partial-exit-code-alignment-coverage | fixed | low | Block 10 covered exit-code alignment for 0, 64 (3x usage error codes), and 1 (packet-render-failed) but not for `ledger-validation-failed` (also exit_code 1). A future drift where the validator returns exit_code 1 in the envelope but the process exits 0 (or vice versa) would not surface. 70 (`unexpected-error`) remains unreachable from the process boundary by design. | Fixed: added a sixth Block 10 test pinning `ledger-validation-failed → process exit 1` using `ledgerNoFrontmatter()` (the deterministic trigger introduced for finding 7). |
| 11 | smoke-misleading-block-4-comment-and-block-10-jsdoc-label | fixed | low | The Block 4 imperative-verb regex carried a comment claiming it matched `cli.test.ts AC3`, but the two verb sets actually differ (AC3's set is `please`, `should`, `must`, `next,` while smoke's set is `ask`, `tell`, `call`, `do`). The top-of-file JSDoc still labelled Block 10 as "Pinned current behaviour (drift residuals)" — the early draft label superseded by the actual `describe()` block name "Process exit code alignment with envelope exit_code". | Fixed: extracted the regex to a module-level `IMPERATIVE_VERB_REGEX` constant with a comment explaining the two surfaces deliberately pin different policies (smoke = minimal action verbs at the JSON-stdout level; AC3 = broader prose-policy lexicon). Updated the top-of-file JSDoc Block 10 label to match the actual `describe()` block name. |
| 12 | smoke-residual-tests-name-misleading | fixed | low | The Block 8 `unexpected-error` test was named "RESIDUAL: unexpected-error is unreachable from the process boundary by design" but its body only asserted catalog membership, not unreachability — a future reader would expect a behaviour pin and find only a documentation pin. | Fixed: renamed to "unexpected-error remains documented in HELP_DATA.error_codes even though the process boundary cannot reach it" and strengthened the body to assert `entry.exit_code === 70`, `entry.severity === "fatal"`, `entry.recoverability === "unrecoverable"` — pinning the documented catalog values for the unreachable-but-documented code. |
| 13 | smoke-stdout-single-envelope-check-was-indirect | fixed | low | Block 9 asserted "exactly one envelope on stdout" via `trimmed.replace(/\n$/, '').includes('\n') === false` — a tolerant proxy that would still pass if the envelope dropped its trailing newline. The documented contract says "newline-terminated". | Fixed: counts newlines explicitly via `(result.stdout.match(/\n/g) ?? []).length === 1` and asserts `result.stdout.endsWith('\n')` to pin the newline-terminator contract. |
| 14 | smoke-dead-code-cleanup-execpath-fallback-and-redundant-reassignment | fixed | low | Two dead-code items: (a) the `bunExecutable` initializer used `execPath` with a logical-OR fallback to the bare string `"bun"`, but `process.execPath` is always set under Bun so the fallback is unreachable; if it ever were falsy, the fallback would resolve a potentially-different binary from PATH. (b) The catch block's `envelopeParsed = false` reassignment was a no-op because the variable defaulted to false and was only set true inside the matching try. | Fixed: dropped the logical-OR fallback; `bunExecutable = execPath` now. Removed the redundant catch-block reassignment. |
| 15 | block-3-confirmed-and-batch-confirmed-fixture-requires-real-digests | closed | low | The original review surfaced a Block 3 "valid confirmed-and-batch-confirmed ledger → route_id matches the current stage" gap. This is the same constraint as ledger row 2 (`block-3-plan-route-fixture-requires-real-digest`): a confirmed ledger requires `ac_digest` and `batch_contract_digest` values that match the computed digests of the ledger's content sections. Static fixtures cannot satisfy this without computing digests at fixture-build time. | Closed: unreachable-from-smoke-by-design. The smoke covers the route classifier surface adequately via no-ledger, pending-AC, blocked-frontmatter, and mismatched-version-skew. The stage-progression routes (pick-batch, build, propose, validate, finalize-pr) remain in-scope of cli.test.ts where digest computation runs in-process. If digest-aware fixtures become important for future smoke coverage, a fixture helper that calls into the v2 digest helper at fixture-build time can be added without changing the contract surface this seam pins. |
| 16 | smoke-packet-happy-path-coverage-for-ledger-bound-roles | closed | low | The first review noted that Block 7 covers `ce-plan` happy path but does not exercise success envelopes for `builder`, `proposer`, `validator`, `patch-proposal` against a real ledger (e.g., asserting `data.role`, `data.packet`, `data.packet_markdown`, `data.dispatch_evidence`). Doing so would require a fixture ledger with valid batches, findings rows, and confirmed-and-batch-confirmed status. | Closed: unreachable-from-smoke-by-design. Same digest-fixture constraint as findings 2 and 15: producing a fixture that survives the ledger-bound packet renderers requires real digests, real batch UUIDs, and matched persona/severity table rows. The cli.test.ts in-process unit tests already cover packet_response_shape against fixtures that compute digests at test time. U10's seam owns the process-boundary envelope shape, not the role-specific success-payload contracts; those are pinned by U5/U6 in-process. |
| 17 | smoke-residual-reliability-and-stage-route-gaps | closed | low | Reliability reviewer surfaced four residual items: (a) 55 sequential subprocess spawns with no retry-with-jitter on transient EAGAIN under CI load; (b) first-non-empty-line stdout parser is fragile to a future Bun version emitting startup noise to stdout; (c) stage-progression routes (pick-batch / build / propose / validate / finalize-pr / etc.) are not exercised at the process boundary; (d) module-global `tempDirs` array is not safe under future intra-file parallelism. | Closed: not-in-u10-scope. (a) and (b) are protections against hypothetical future Bun/CI behaviour — current behaviour is correct and the runbook anti-list does not call them out. (c) is the same digest-fixture constraint as finding 15. (d) is latent against future Bun versions; Bun runs tests sequentially within a file today. Each item is a candidate for a future hardening seam if CI flake patterns or Bun behaviour change. |
| 18 | block-3-state-shape-pin-missing-five-documented-fields | fixed | low | Pass-2 review (correctness + testing + api-contract converged) flagged that Block 3's state_response_shape pin asserted 12/17 documented fields but omitted `plan_path`, `has_batches`, `all_batches_terminal`, `final_reviewed_at`, `pr_url`. All five are emitted unconditionally by `cli.ts` state command and documented in `HELP_DATA.state_response_shape`. Resolution-row-4 claimed full coverage but missed these five. | Fixed: extended the Block 3 shape pin with type checks for all five remaining documented fields. `has_batches` and `all_batches_terminal` pinned as booleans; `plan_path`, `final_reviewed_at`, `pr_url` pinned as nullable-string (matching the documented "string or null" schema). |
| 19 | smoke-runbook-version-type-pin-narrower-than-schema | fixed | low | Pass-2 review flagged that `runbook_version` and `runbook_version_skew` type pins (Block 3 + Block 6) asserted `typeof === "string"` but the documented schema reads "string or null when missing / no-ledger". The pins narrowed the documented contract to a single nullability variant. | Fixed: widened both pins (state and diagnose blocks) to `expect(["string", "object"]).toContain(typeof field)` — matches the documented "string or null" contract without over-pinning. |
| 20 | block-9-event-pin-over-specifies-optional-schema-field | fixed | low | Pass-2 testing review flagged Block 9's diagnostic-separation assertions asserted `typeof record.event === "string"` for every stderr line, but `CliDiagnosticRecord.event` is optional in the documented record shape (`lib/cli-diagnostics.ts:154`: `event?: string`). The pin would fail against a future spec-compliant info-level emit that omits `event` — even though that emit would not violate the documented contract. | Fixed: dropped the `typeof record.event === "string"` assertions in both diagnostic-separation tests. Kept the disjoint-shape pins (`level` is string, `status`/`schema_version` are absent) so the contract that matters — diagnostic records are not envelopes — stays pinned. |
| 21 | block-9-stdout-contamination-check-was-brittle-substring | fixed | low | Pass-2 testing review flagged `expect(result.stdout).not.toContain("event")` as a cross-stream contamination guard. The envelope has no `event` top-level key today so it passes, but a future envelope field whose name contains the substring `event` (e.g. `event_id`) would fail without any actual contamination. | Fixed: replaced the substring check with a JSON-parse round-trip: parse the stdout line as JSON and assert no `event` top-level key. Behaviour-anchored rather than substring-anchored. |
| 22 | parameterized-loop-failure-context-loss | fixed | low | Pass-2 testing review flagged that Block 2 (commands), Block 5 (slices), and Block 7's required-flag matrix iterate via `for...of` over runtime-derived catalogs. When a single iteration fails, bun test's failure message shows only the bare `expect()` failure with no indication of which command/slice/role/flag combination triggered it — forcing the reader to bisect locally. | Fixed: each iteration's failure paths now `throw new Error("[iter=...] ...")` with the iteration variable embedded in the failure message before delegating to the shared `assertErrorEnvelopeShape` helper. A failing iteration now surfaces its iteration coordinates directly in the test output. |
| 23 | nullable-string-pin-accepts-arrays-and-objects | fixed | low | Pass-3 review flagged that the row-19 fix used `expect(["string", "object"]).toContain(typeof field)` to widen pins for documented nullable-string fields (runbook_version, runbook_version_skew, plan_path, final_reviewed_at, pr_url — all documented as "string or null"). Because `typeof null === "object"` AND `typeof []` / `typeof {}` are also `"object"`, the pin accepts arrays and bare objects that the documented schema does not permit. | Fixed: introduced `assertNullableString(value, label)` helper that asserts the value is either `null` or a string, throwing with the field label embedded for diagnosability. Replaced all 7 widened `typeof`-based pins in Block 3 + Block 6 with `assertNullableString` calls. |
| 24 | iteration-context-lost-on-shape-helper-failures | fixed | low | Pass-3 review flagged that the row-22 fix attached iteration coordinates only to envelope-parse failures + Block 7's bespoke error.code mismatch check, but NOT to downstream assertions inside `assertErrorEnvelopeShape` (exit_code, severity, recoverability, retryable, hint.action) or to Block 5's post-parse success-shape assertions (data.slice, data.values, data.ordering). A failing iteration on any of those paths still surfaced as a bare `expect()` message without iteration coordinates. | Fixed: `assertErrorEnvelopeShape` now accepts an optional `context` parameter; on internal expect() failure it appends ` [context]` to the thrown error message. Block 2 and Block 7 pass their iteration variables as the context string. Block 5's success-shape assertions are wrapped in a try/catch that re-throws with the slice name appended. A failing iteration in any helper path now surfaces its iteration coordinates directly. |
| 25 | block-10-envelope-error-access-without-parse-guard | fixed | low | Pass-4 review flagged that four Block 10 tests (missing-command, unknown-command, packet-render-failed, ledger-validation-failed) read `result.envelope.error as Record<string, unknown>` and accessed `error.exit_code` / `error.code` without first checking `result.envelopeParsed`. Every other test in Block 8/Block 2/Block 7 guards either explicitly or via `assertErrorEnvelopeShape`. If a future change caused cli.ts to emit stdout noise on one of these paths, the test would fail with `TypeError: Cannot read property 'exit_code' of undefined` instead of a clean envelopeParsed assertion. | Fixed: added explicit `expect(result.envelopeParsed).toBe(true)` to all four Block 10 tests before any `envelope.error` access. Diagnosability discipline now consistent across the entire file. |
| 26 | assert-nullable-string-undefined-stringify-crash | fixed | low | Pass-4 review flagged that `assertNullableString` (row 23's helper) constructs its error message via `JSON.stringify(value).slice(0, 80)`. `JSON.stringify(undefined)` returns the literal `undefined` (not a string), so `.slice` would throw `TypeError: Cannot read property 'slice' of undefined`. Same is true for symbols and bigints. Latent today (no documented nullable-string field returns undefined), but a future field omission or type change would crash inside the helper with a confusing stack rather than the intended diagnostic message. | Fixed: guarded with `JSON.stringify(value) ?? String(value)`. The helper now surfaces `expected ... to be string or null, got undefined (undefined)` correctly for any unexpected value type. |
| 27 | block-9-default-stderr-threshold-comment-missing | fixed | low | Pass-4 review flagged that Block 9's "stderr empty by default" pin (unknown-command, success path) and "error-level diagnostics on stderr" pin (ledger-validation-failed) pin opposite sides of the same MODE_MIN_LEVEL threshold contract. The threshold mapping was not commented, so a future reader would not immediately understand why one test asserts empty stderr while the next asserts non-empty stderr on different error paths. | Fixed: expanded the comment on the "stderr empty by default" test to name the threshold contract being pinned (`unknown-command` short-circuits BEFORE `command.fail` emit; `command.fail` is error-rank and lands on stderr by default). The two pins are now self-documenting as opposite sides of the threshold contract. |
