# U6 runbook versioning - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| U6-001 | runbook-version-constant-and-type-export | fixed | low | `RUNBOOK_VERSION = "2"` and `RunbookVersion` type exported from `lib/contract.ts`; covered by `contract.test.ts` U6 describe block. | Shipped in 5428917 scaffold; verified by 940-suite green sweep. |
| U6-002 | ledger-snapshot-runbook-version-classifier | fixed | medium | `LedgerSnapshot` carries `runbook_version: string \| null` and `runbook_version_skew` with the four-state enum (matched / missing / mismatched / continuation-evidence-present). | Implemented in `lib/ledger.ts` via `readFrontmatterRunbookVersion` + `classifyRunbookVersionSkew`; tested at `lib/ledger.test.ts` U6 describes covering matched / missing / mismatched / continuation cases plus string-only comparison. |
| U6-003 | continuation-evidence-parser-required-fields | fixed | high | `parseRunbookVersionContinuationEvidence` requires all seven documented fields and rejects partial / forged / duplicate-field / extra-field rows. | Implemented in `lib/ledger.ts`; tested at `lib/ledger.test.ts` parser describe with twelve cases including missing-each-field loop and hostile-marker spoofing. |
| U6-004 | installed-artifact-presence-recursive-walk | fixed | medium | `installedArtifactPresence` performs a real recursive walk against the v2 install root, returns `{ references, templates, cli_ts, lib_dir, all_present, missing }`. Handles symlink loops via visited-realpath set + depth cap; non-existent paths collapse to all-missing. | Implemented in `lib/route.ts`; tested at `lib/route.test.ts` U6 describe with five cases (default invocation / non-existent / partial / empty-subdir / nested-empty-subdir / no-file-list leak). |
| U6-005 | cli-state-diagnose-additive-surface | fixed | medium | `cli.ts state` and `cli.ts diagnose` surface `runbook_version`, `runbook_version_skew`, and the new `installed_artifact_presence` shape additively. U4 envelope shape (status / schema_version "1" / run_id / started_at_ms / duration_ms) preserved. | Implemented in `cli.ts`; tested at `cli.test.ts` U6 describes covering state matched / missing / mismatched / continuation / partial-evidence cases, diagnose mirror, and envelope-shape preservation pin. |
| U6-006 | version-skew-stop-required-gate | fixed | high | `blockingGatesFor` accepts `runbook_version_skew` and emits a `version-skew-stop-required` field gate when skew is missing or mismatched. `classifyRoute` returns `blocked-runbook-version-skew` precedence-correctly. | Implemented in `lib/route.ts`; tested at `lib/route.test.ts` (gate emission for missing / mismatched / continuation-suppression / matched / null) and via the CLI surface tests above. |
| U6-007 | issue-N-ledger-template-v2 | fixed | low | `runbooks/issue-to-pr-v2/issue-N-ledger.template.md` ships with `runbook_version: "2"` frontmatter and an authoritative Notes evidence shape section documenting the `<!-- runbook-version-skew-continuation -->` marker + required fields. | New file created; mirrors v1 template structure and adds the U6 evidence shape inline. |
| U6-008 | references-ledger-and-helper-skew-doc | fixed | low | `references/ledger-and-helper.md` documents the four-state classifier table, the continuation evidence shape, and the version-skew blocking gate semantics. Updates the existing blocked-runbook-version-skew row to reflect the U6-shipped behavior. | Targeted edit, no restating of U2 prose. |
| U6-009 | references-host-adapters-install-presence | fixed | low | `references/host-adapters.md` documents the `installedArtifactPresence` contract (allow-list shape, deny-list leakage). Cross-link to ledger-and-helper.md skew classifier added. | Targeted edit. |
| U6-010 | install-sh-status-v2-presence | fixed | low | `install.sh --status` invokes a new `check_v2_artifact_presence` block that walks references/, templates/, cli.ts, lib/ recursively (with `find -L` to honor the symlink topology) and reports PRESENT / EMPTY / MISSING per artifact. No `cp -r` install path introduced. | Verified by running `./install.sh --status`; output shows the v2 block with all four artifacts PRESENT. |
| U6-011 | installed-presence-misses-symlink-to-file-leaves | fixed | medium | `walkHasFile` now short-circuits via `statSync(...).isFile()` when an entry is a symlink to a regular file, so directories containing only symlink-to-file leaves classify as present. Broken symlinks are skipped without poisoning the walk. | Implemented in `lib/route.ts`; tested at `lib/route.test.ts` with dedicated symlink-to-file and broken-symlink cases. |
| U6-012 | continuation-marker-inside-fenced-block-honoured | fixed | high | Walker tracks open column-zero fences and refuses to honor a marker that lives inside one. Only legitimate (marker → `<!--...-->` then `\`\`\`yaml` at column zero, both unfenced) sequences enter the evidence parser. | Implemented in `lib/ledger.ts`; tested at `lib/ledger.test.ts` F-U6-SEC-002 case and via the CLI in `cli.test.ts` (nested-fence-smuggled-marker test). |
| U6-013 | notes-section-heading-not-anchored-to-line-start | fixed | medium | `## Notes` regex now requires column-zero anchor `(?:^\|\n)##\s+Notes`, so a `> ## Notes` blockquote or other inline occurrence cannot fabricate a Notes scope. | Implemented in `lib/ledger.ts`; tested at `lib/ledger.test.ts` F-U6-SEC-001 case. |
| U6-014 | string-comparison-vulnerable-to-unicode-whitespace | fixed | medium | `readFrontmatterRunbookVersion` strips NBSP / ZWSP / BOM boundaries via `stripBoundaryWhitespace` so visually-identical-but-not-equal padded versions classify correctly. Continuation evidence runtime_version / ledger_version are stripped symmetrically. | Implemented in `lib/ledger.ts`; tested at `lib/ledger.test.ts` F-U6-SEC-013 case (four padded variants). |
| U6-015 | evidence-control-byte-injection | fixed | medium | `runbook_version` and every evidence field containing a C0/C1 control byte (sans tab) is rejected. `containsControlByte` is implemented with a code-point scan to avoid embedding control bytes in a regex (biome lint). | Implemented in `lib/ledger.ts`; tested at `lib/ledger.test.ts` F-U6-SEC-005 cases (control byte in evidence field + in frontmatter runbook_version). |
| U6-016 | version-skew-stop-required-kebab-case-drift | fixed | low | The version-skew blocking gate field identifier is renamed from `version-skew-stop-required` (event-name kebab-case) to `frontmatter.runbook_version` (dotted ledger-field path), aligning with the existing gate vocabulary (`frontmatter.status`, `ac_confirmation_status`, `batch_contract_confirmation_status`). The U7 prose retains the "version-skew-stop-required" event name. | Implemented in `lib/route.ts`; tests updated in `lib/route.test.ts` and `cli.test.ts`; documented in `references/ledger-and-helper.md`. |
| U6-017 | help-data-state-diagnose-response-shape | fixed | low | `HELP_DATA` now exposes `state_response_shape` and `diagnose_response_shape` documenting every field name and value vocabulary for the two ledger-reading commands. Agents can route off `--help --json` alone. | Implemented in `cli.ts`; tested at `cli.test.ts` HELP_DATA case asserting key documentation content. |
| U6-018 | contract-slice-runbook-version-skew-states | fixed | low | New `RUNBOOK_VERSION_SKEW_STATES` set exported from `lib/contract.ts`; new `runbook_version_skew_states` slice surfaced through `contract --json` with `ordering: catalog`. | Implemented in `lib/contract.ts` + `cli.ts`; tested at `cli.test.ts`. |
| U6-019 | installed-presence-file-and-directory-misplacement-coverage | fixed | low | Dedicated tests cover the regular-file-at-directory-root and directory-at-cli_ts paths so future regressions in the `fileExists` / `directoryHasAnyFile` branches surface immediately. Isolated symlink-loop test added with a bounded-duration assertion. | Tests added in `lib/route.test.ts`. |
| U6-020 | cli-determinism-skew-classifier-pin | fixed | low | New CLI test runs `state` twice against a mismatched + continuation-evidence ledger and asserts `runbook_version`, `runbook_version_skew`, and `blocking_gates` are deep-equal across runs. Catches any future fs-order or first-marker iteration drift. | Tests added in `cli.test.ts`. |
| U6-021 | cli-verbatim-runbook-version-pin | fixed | low | CLI test confirms `runbook_version: "2.0-beta"` flows through the envelope verbatim with `typeof === "string"`. Catches a future serializer regression that would coerce versions to numbers. | Tests added in `cli.test.ts`. |
| U6-022 | evidence-runtime-version-stale-diagnostic | open | low | When evidence carries a runtime_version that does NOT match the current `RUNBOOK_VERSION`, the snapshot correctly falls back to `mismatched` / `missing` skew. The CLI envelope does not separately signal "stale evidence present" — diagnose users currently cannot distinguish "no evidence" from "evidence too old". Severity P1 in the correctness sweep but accurate enum response means routing is correct today. | closed: deferred-to-u7-router — orchestrator's user-facing message on stop-required skew (including stale-evidence guidance) is U7's prose responsibility. |
| U6-023 | quoted-string-null-as-literal-string-template-ambiguity | fixed | low | Template at `issue-N-ledger.template.md` now explicitly documents that `ledger_version` must be bare `null` (no quotes) when frontmatter is absent, and that `"null"` (quoted) is treated as a literal four-char string. | Template prose updated. |
| U6-024 | installed-artifact-presence-shape-rename | closed | low | `installedArtifactPresence()` shape changed `cli/lib` → `cli_ts/lib_dir` + added `all_present`/`missing` under the same envelope `schema_version: "1"`. Pre-U6 the diagnose envelope shipped the old shape in U4; no live consumer existed before U7 since the hot router was not yet wired. | closed: out-of-scope-for-this-issue — the U6 spec defines the new shape as the contract and the U4 envelope schema_version applies to the envelope wrapper (status / run_id / etc.) not to nested per-command `data` shapes that evolve with the U6+ contracts. |
| U6-025 | install-presence-walk-symlink-to-arbitrary-tree | open | low | Symlinking `runbooks/issue-to-pr-v2/references` to an arbitrary tree (e.g. `/etc`) would let `installedArtifactPresence` and `install.sh --status` return PRESENT based on unrelated files. | closed: accepted-risk — an attacker who can rewrite the install topology can already escalate beyond what the v2 helper is designed to defend against. The structured envelope leaks no paths/sizes/inodes either way. Documented as a known limitation; revisit if install topology grows. |
| U6-026 | ledger-size-cap-not-enforced | open | low | `readFileSync` on the ledger has no size cap; a multi-GB ledger would OOM the helper. | closed: accepted-risk — bounded by node fs limits and the v1 helper has the same shape; if it bites, fix at the fs read layer for both helpers. |
| U6-027 | runbook-version-equality-test-against-imported-constant | open | low | No test mutates `RUNBOOK_VERSION` at runtime to prove `classifyRunbookVersionSkew` compares the imported symbol rather than a hardcoded literal. | closed: deferred-to-u9-probes — regression probe that exercises the constant-rebump path belongs to U9. Today the coupling is correct by construction (lib/ledger.ts imports `RUNBOOK_VERSION` from `lib/contract.ts`). |
| U6-028 | broader-hostile-marker-spoofing-table-test | open | low | Hostile-marker test verifies one attack vector (bare prose marker); the spec mentions several other variants (whitespace-prefixed marker, marker in code block, split across lines). | closed: deferred-to-u9-probes — F-U6-SEC-001 + F-U6-SEC-002 fixes plus the column-zero scanner short-circuit all of the listed variants by construction. A table-driven exhaustive test belongs to the regression probe sweep. |
| U6-029 | runbook-version-skew-states-tuple-order-disagrees-with-spec | fixed | low | Sweep 1 introduced `RUNBOOK_VERSION_SKEW_STATES` in precedence order; the U6 spec enumerates the four states in a different order. Reordered the tuple to match the spec (`matched, missing, mismatched, continuation-evidence-present`). | Implemented in `lib/contract.ts`; test pin in `cli.test.ts` updated. |
| U6-030 | route-ts-jsdoc-legacy-gate-name | fixed | low | `blockingGatesFor` JSDoc led with `version-skew-stop-required` as the wire identifier. Updated JSDoc + inline comment to lead with the actual wire field `frontmatter.runbook_version` and demote the event-name reference to a parenthetical. | Doc-only change in `lib/route.ts`. |
| U6-031 | blocking-gate-field-name-vocabulary-missing-contract-slice | fixed | medium | Added `BLOCKING_GATE_FIELD_NAMES` tuple + `BlockingGateFieldName` type in `lib/route.ts`. Surfaced via new `blocking_gate_field_names` contract slice. The `BlockingGate.field` discriminant is now narrowed to the typed enum. | Tested at `cli.test.ts` with a slice-shape assertion. |
| U6-032 | evidence-marker-blank-line-between-marker-and-yaml-fence | fixed | medium | Sweep 1 fix required marker on the immediately-previous line, diverging from the template prose that allows blank lines. Replaced with `findPrecedingMarker` that walks past blank lines so the parser matches the template contract. | Implemented in `lib/ledger.ts`; tested at `lib/ledger.test.ts` "template-documented blank line" case. |
| U6-033 | evidence-walker-rejected-row-then-valid-second-row | fixed | low | New regression test pins the recovery branch where an invalid evidence row precedes a valid one in the same Notes section. Catches a future refactor that would short-circuit on first reject. | Test added in `lib/ledger.test.ts`. |
| U6-034 | findClosingFenceIndex-eof-no-closing-fence | fixed | low | New test exercises the EOF branch where the evidence fence opens but never closes; asserts bounded completion (no hang) and null return. | Test added in `lib/ledger.test.ts`. |
| U6-035 | containsControlByte-boundary-codepoints | fixed | low | New parameterised test pins the C0/C1 boundary code points (0x08, 0x09, 0x1F, 0x7F, 0x9F, 0xA0) through the public `runbook_version` reader. Future off-by-one in the range check fails fast. | Test added in `lib/ledger.test.ts`. |
| U6-036 | longer-closing-fence-commonmark-variant | open | low | Sweep 2 noted that the `findClosingFenceIndex` `length >= openFence.length` branch (CommonMark §4.5 longer-closing-fence) is not exercised. | closed: deferred-to-u9-probes — the fence-length comparison is exercised implicitly by every balanced 3-backtick test and the EOF test; a longer-closing-fence regression test is a U9 probe candidate. |
| U6-037 | named-unicode-padding-tests-per-character | open | low | Sweep 2 suggested splitting the NBSP/ZWSP/BOM padded-version test into named `test.each` variants for better failure diagnostics. Existing test loop covers all three characters; bug regressions would still fail the assertion (just with a less specific message). | closed: deferred-to-u9-probes — diagnostic improvement only; coverage already exists. |
| U6-038 | state-response-shape-blocking-gates-doc-string | fixed | low | Sweep 3 noted that the HELP_DATA `state_response_shape.blocking_gates` description still said `field: string` after U6-031 narrowed `BlockingGate.field` to `BlockingGateFieldName`. Updated the description to reference the `blocking_gate_field_names` slice, mirroring how `route_id` is described as `one of route_ids`. | Doc-only change in `cli.ts`. |
