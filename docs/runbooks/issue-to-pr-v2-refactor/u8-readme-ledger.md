# U8 v2 README - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 1 | invocation-bun-cannot-execute-markdown | fixed | low | README invocation example `bun ~/.claude/runbooks/issue-to-pr-v2/issue-to-pr.md <issue-number>` is not runnable: bun is a JS/TS runtime and cannot execute markdown. The v1 README invokes the hot router via `/goal Follow ~/.claude/...`, not `bun <file>.md`. | Replaced bash example with `/goal Follow ~/.claude/runbooks/issue-to-pr-v2/issue-to-pr.md ...` driver invocation mirroring the v1 README shape. |
| 2 | lib-route-ids-ownership-claim-wrong | fixed | low | README lib/contract.ts entry claims it owns "route ids, confirmation states, RUNBOOK_VERSION, packet roles". `ROUTE_IDS` lives in `lib/route.ts`; `PacketRole` lives in `lib/packets.ts`. | Trimmed `lib/contract.ts` entry to "runtime contract constants shared across the CLI and helper surfaces" — no enumeration of which constants live where. |
| 3 | ownership-table-runtime-contracts-claim-wrong | fixed | low | "Ownership at a glance" row attributes route ids to `lib/contract.ts + the packet templates`, repeating the same wrong attribution. | Deleted the "Ownership at a glance" table entirely (also resolves findings 4 and 8). |
| 4 | readme-claims-ownership-of-compatibility-behaviour | fixed | low | "Ownership at a glance" lists "this README" as a co-owner of compatibility behaviour, contradicting the README's finder role and creating a competing source of truth. | Resolved by deleting the ownership table; no remaining co-ownership claim. |
| 5 | readme-enumerates-installed-artifact-presence-sub-fields | fixed | low | README L20-22 enumerates `installed_artifact_presence.all_present`, `references`, `templates`, `cli_ts`, `lib_dir`, `missing` sub-field names. Spec MUST NOT leak section forbids enumerating envelope sub-fields. | Trimmed "Installed path" section to point at `references/host-adapters.md#install-artifact-presence-u6` for the structured map; no envelope sub-fields enumerated in the README. |
| 6 | readme-lib-section-names-internal-module-contents | fixed | low | README lib/ entries name specific exports (`classifyRoute`, `installedArtifactPresence`, "four-state runbook-version skew classifier") instead of one-line roles. Spec says "README does NOT restate the public API of any module". | Tightened every lib/ entry to a one-line role; removed named exports and the four-state skew classifier reference. |
| 7 | readme-cli-section-names-packet-roles | fixed | low | `cli.ts packet` description lists all five dispatch role names (Builder, Proposer, Validator, patch-proposal, ce-plan). Role names belong with `templates/` and findings-and-validators.md, not the README. | Trimmed `packet` command summary to "render a dispatch-role packet from `templates/` and ledger state"; added pointer to `references/findings-and-validators.md` in the templates/ entry where role names live. |
| 8 | ownership-table-redundant-with-file-map-and-anti-list | fixed | low | "Ownership at a glance" table duplicates the concern-to-artifact mapping already encoded in the File map and the "What this area deliberately does not do" anti-list. Three sections to keep in sync. | Deleted the table (same fix as finding 3). |
| 9 | compatibility-notes-restates-shared-ledger-path | fixed | low | Compatibility-notes bullet restates the shared ledger path and `runbook_version: "2"` v1/v2 split inline; ledger schema and runbook_version contract live in `references/ledger-and-helper.md` (R11). | Trimmed Compatibility-notes bullets to one-sentence pointers to the new "Per-issue ledger" section and `references/ledger-and-helper.md`. |
| 10 | scenario-finds-ledger-location-not-in-headings | fixed | low | "Ledger" is absent from every README heading. A maintainer scanning headings cannot land directly on ledger location; they must read file-map item 7 body. | Added a top-level `## Per-issue ledger` heading naming the path and pointing at the ledger schema reference. |
| 11 | regression-matrix-missing-u8-finder-probes | closed | low | Regression matrix has only one U8-owned row (`readme-invocation-driver-and-index`, a v1->v2 content-mapping row), with no finder-probe anchors for the four plan-listed scenarios. | Closed: `deferred-to-u9-probes` per the U8 runbook's closing reasons — U8 documents what exists; U9 owns regression probes. |
| 12 | installed-path-state-example-omits-ledger-arg | fixed | low | README Installed-path section showed `cli.ts state --json` as the install-presence check, but `state` requires `<ledger-path>` and exits 64 without it. A maintainer running it pre-ledger would hit `missing-required-arg`. | Reworded to `cli.ts state <ledger-path> --json reports installed_artifact_presence.all_present` so the prose names the required arg and the spec-required envelope field. |
| 13 | readme-uses-install-topology-prose-instead-of-installed-artifact-presence-field-name | fixed | low | README prose said "install-topology fields from the returned envelope" instead of naming `installed_artifact_presence` (the actual envelope field used by host-adapters.md, ledger-and-helper.md, and cli.ts). The U8 spec MUST-include section explicitly names this field. | Reworded the Installed-path paragraph to cite `installed_artifact_presence.all_present` verbatim, matching the spec's MUST-include language. |
| 14 | install-topology-pointer-duplicated-across-installed-path-and-compatibility-notes | fixed | low | Two sections pointed at `references/host-adapters.md#install-artifact-presence-u6` for the same install-topology fact ("Installed path" + Compatibility-notes "Symlink topology" bullet). Same pointer in two homes is the structural redundancy this seam avoids. | Deleted the Compatibility-notes "Symlink topology" bullet. "Installed path" owns the pointer. |
| 15 | dispatch-role-names-pointer-too-vague | fixed | low | File-map templates entry said "Dispatch role names live alongside these templates and in `references/findings-and-validators.md`", but findings-and-validators.md has no dedicated "Dispatch roles" section — role names appear inline across multiple subsections. Pointer failed to name an exact owning section. | Reworded to "The role each template serves is encoded in its filename; the validator persona model lives in `references/findings-and-validators.md`" — pointer now matches what that artifact actually owns (the persona model). |
| 16 | ledger-schema-pointer-stated-three-times-in-readme | fixed | low | README pointed at `references/ledger-and-helper.md` three times for the ledger schema (Per-issue ledger section, file-map item 7, anti-list ledger schema bullet). Two were redundant. | Trimmed file-map item 7 to a back-reference to the "Per-issue ledger" section above; ledger schema pointer now lives in one section + one anti-list bullet only. |
| 17 | host-adapters-see-also-bullet-now-carries-rule-body-fragment | fixed | low | host-adapters.md See-also bullet had a parenthetical "(helpers run from the target repo root)" — a rule-body fragment inside what should be a pointer-only bullet. | Dropped the parenthetical; the link text "canonical helper execution context rule" carries the navigation cue without restating the rule. |
