# U7 hot router - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| F001 | u7-install-presence-not-emitted-as-blocking-gate | fixed | low | Install-presence gate is enforced by prose only; classifier never elevates partial install to a blocked-* route or a blocking_gates field entry. | Added an explicit note in the hot file's Pre-stage gates section calling out that install-presence is a sibling-field gate (NOT a blocking_gates entry) and that prose enforcement is procedural. Widening blockingGatesFor would change the CLI envelope shape and is out of U7 scope per anti-list. |
| F002 | u7-stage-1-cli-facts-claim-confirmation-state-after-create | fixed | low | Stage-1 shell prose claimed `confirmation_state.digests: "pending"` post-create, but the helper-computed digest state after a Stage-1 ledger (no plan, no batches) may resolve differently. | Verified against lib/ledger.ts readLedgerSnapshot: with `plan_path: null` and empty batches the helper reports `digests: "pending"` because no stored digest exists to compare against. Prose is consistent with the executable contract; no edit needed beyond aligning Stage-1 narration with the helper-recompute rule (also covered by F008). |
| F003 | u7-clonerecord-shallow-clone-not-defensive-for-nested-attrs | fixed | low | cloneRecord in cli-diagnostics.ts was a shallow spread, so a redactor that mutated a nested attribute object would corrupt the carrier and leak shared state. | Switched to structuredClone for deep-clone semantics; redactors now operate on a fully independent record. JSDoc updated to declare deep clone. |
| F004 | u7-redactor-must-return-cli-shape-not-enforced | fixed | low | buildLogTapeSink did not re-validate the redactor's return value, so a misbehaving redactor could drop required fields and produce malformed JSON. | Added isCliDiagnosticRecord re-validation after redactor invocation; drops the record on shape failure. |
| F005 | u7-resetclidiagnostics-resets-global-logtape | fixed | low | resetCliDiagnostics calls LogTape's process-global reset() unconditionally, potentially wiping other tenants in the same process. | Added explicit JSDoc note that the v2 CLI owns the process-global LogTape config (acceptable for a CLI process; this is the documented sidequest pattern). |
| F006 | stage-4-version-skew-gate-prose-fabricates-field-on-installed-presence | fixed | low | Stage-4 install-presence prose did not inline the typed `missing: ('references'\|'templates'\|'cli_ts'\|'lib_dir')[]` literal union. | Inlined the literal union in stage-4-batch-loop.md's pre-stage gates section. |
| F007 | hot-file-installed-presence-omits-missing-from-field-list | fixed | low | Hot file's install-presence narration enumerated fields across two paragraphs rather than once. | Inlined the full `{references, templates, cli_ts, lib_dir, all_present, missing[]}` tuple in the hot file's gate section. |
| F008 | stage-4-blocking-gates-field-name-narrowing-implicit | fixed | low | Stage shells referenced `blocking_gates` without naming the discriminated-union shape or the BLOCKING_GATE_FIELD_NAMES tuple. | Added a one-sentence narration of the discriminated union and the four field-name tuple to the hot file's Pre-stage gates section. |
| F009 | stage-3-helper-recompute-orchestrator-inline-edit-contradiction | fixed | low | Stage-1 prose said digests stay null at Stage 1 and "the helper recomputes them"; Stage-3 prose said the orchestrator stores them inline. | Reconciled: Stage 1 now says the digests are computed at Stage 3 confirmation via the named helper commands and persisted by the orchestrator at the same checkpoint; Stage 3 keeps the explicit persist step with helper command names. |
| F010 | hot-file-shipped-route-no-required-references-mismatch | fixed | low | Hot file's Reference loading table had no row for the terminal `shipped` route. | Added a "Terminal — no references required" row for `shipped`. |
| F011 | new-lib-modules-not-just-upgrade | closed | low | Reviewer noted the U7 seam anti-list phrase "file already exists from U4" is unverifiable against `main` because the v2 lib tree was born on the long-running feature branch. | Closed as not-in-u7-scope: the finding is against the seam-runbook authoring convention, not the U7 implementation. cli-diagnostics.ts was created in U4 commit 61ef2e1; U7 modifies its emitter under the hood without changing the public record shape. Wording can be tightened when the parent issue-to-pr-v2 refactor PR merges and the U4/U7 commits become a single mainline lineage. |
| F012 | u7-logtape-inheritance-test-asserts-only-leaf-categories-not-true-parent-subscription | fixed | low | Existing inheritance test installed a single sink at the root category, so it would have passed even with flat (non-inherited) routing. | Upgraded the test to register an intermediate-category sink (`issue-to-pr-v2.cli`) and assert that it receives `cli.*` records but NOT `lib.*` records, while the root sink receives both. |
| F013 | u7-redactor-test-uses-credential-pattern-but-not-routed-as-builder-commit-ref-or-finding-summary-via-context-bundle | fixed | low | Redactor test only exercised top-level string attributes; the deep-clone contract from F003 was not pinned. | Added a test that mutates a NESTED attribute object inside the redactor and asserts the original record passed to the LogTape sink is unaffected (proving structuredClone isolation). |
| F014 | u7-no-test-confirms-u4-record-shape-preserved-byte-for-byte-through-logtape-path | fixed | low | No test compared the key set produced by the U4 direct-to-stderr path against the LogTape path. | Added a parity test that emits the same EmitDiagnosticInput through both paths (configured + reset) and asserts Object.keys equality plus identical values on every reserved U4 field. |
| F015 | u7-install-presence-gate-not-covered-by-route-classifier-tests-only-by-installed-artifact-presence-walker | closed | low | classifyRoute does not read installed_artifact_presence; the install-presence gate is enforced by hot-router prose only. | Closed as not-in-u7-scope: widening classifyRoute or blockingGatesFor would change the CLI envelope shape and routing semantics, both of which the U7 anti-list forbids. The complementary F001 fix documents this seam-boundary explicitly so future implementations can route uniformly. The gate's behaviour is covered by F019 (regression-test request) and by deferred U9 probes. |
| F016 | u7-stale-digest-three-axis-precedence-tested-but-not-stage-3-routing-after-stale-digest-with-batches-present | fixed | low | classifyRoute coverage missed the case where digests drift after final-review readiness (`all_batches_terminal: true`, `digests: "stale"`). | Added a route.test.ts regression that asserts `blocked-digests-stale` wins over `final-review` and `ship` when digests drift; pins scenario 4 of the U7 audit prompt. |
| F017 | u7-stage-5-p0-p1-patch-routing-no-test-pins-final-review-to-stage-4-patch-batch-flow | closed | low | No test pins the Stage 5 → Stage 4 patch-batch routing decision. | Closed as deferred-to-u9-probes: the patch-batch flow is an orchestration-prose decision (not a classifier output), and exercising it requires the hot-router invocation harness that U9 owns. The static reference list (`final-review` → stage-4-batch-loop.md) IS pinned in route.test.ts; the dynamic invocation is U9's. |
| F018 | u7-resumed-run-happy-path-no-builder-or-patch-detail-not-explicitly-tested | closed | low | No explicit test exercises the "resumed turn determines next action from cli.ts state envelope alone" invariant. | Closed as deferred-to-u9-probes: classifyRoute is a pure function over RouteInputs and structurally cannot read out-of-band Builder details; the invariant is enforced at the type level. A behavioural probe that runs the hot router under a sandbox where Builder paths throw on read belongs to the U9 probe harness. |
| F019 | redactor-uncaught-throw-on-fallback-path | fixed | low | A throwing redactor inside buildLogTapeSink would emit a LogTape meta `fatal` carrying the unredacted record; today nothing receives it (only `cli` sink and it's bypassed) but the failure mode would surface if a second sink were ever added. | Wrapped `configured.redact(...)` in a local try/catch inside buildLogTapeSink. On throw, the record is dropped and the failure is surfaced via a single direct stderr write (bypassing LogTape entirely to avoid re-entrancy). Added a unit test asserting a throwing redactor writes nothing to the configured writer and produces no meta-record leakage. |
| F020 | hostile-redactor-can-widen-record-shape | fixed | low | Same root as F004 from a security perspective: a redactor that returned a malformed record was not re-validated. | Covered by the F004 isCliDiagnosticRecord re-validation; an explicit security-perspective test asserts a redactor that strips run_id produces no output. |
| F021 | fallback-path-emits-without-redaction | fixed | low | callers that never call configureCliDiagnostics get the U4 direct-to-stderr stream with no redactor; this is opt-in by design but was only documented in the file header. | Surfaced the opt-in contract explicitly in the emitDiagnostic JSDoc and the JSDoc on emitDiagnostic's `stderr` parameter. The CLI entry point (`cli.ts`) is unchanged for U7; installing a default redactor at the CLI entry is a U8/U9 concern (the entry-point ownership boundary). |
| F022 | meta-logger-cli-sink-coupling | fixed | low | If a second sink were ever added, meta records from caught redactor throws would carry the unredacted record. | Covered defensively by F019 (the redactor exception is caught in our sink before LogTape's catch path sees it); the meta path no longer carries our unredacted record. Reviewer-noted "defence in depth" achieved. |
| F023 | test-example-redactor-uses-stateful-global-regex | fixed | low | The redactor test used `/ghp_[A-Za-z0-9]{36}/g` with `.test()` in a loop — a stateful-lastIndex footgun that would intermittently miss credentials on second invocation. | Rewrote the test redactor to use a non-stateful pattern (no `g` flag; `.replace()` directly) so the example is safe to copy. |
