# U5 packet rendering - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| F001 | builder-notes-substring-leak | fixed | P0 | extractNotesForBatch uses unbounded substring matching on batchId; leaks Notes lines that mention other batches when ids are substrings (b1 vs b10) or appear as substrings in unrelated prose | commit pending: word-boundary regex in extractNotesForBatch + adversarial b1/b10 fixture in packets.test.ts |
| F002 | proposer-swallows-ledger-validation-error | fixed | P1 | tryReadLedgerBatchContext catches every error from readLedgerBatchContext and silently returns null; conflates "no confirmed batches" with "malformed Batches block" | commit pending: narrowed catch to "has no confirmed batch ids" message; all other errors re-throw |
| F003 | patch-proposal-accepts-wildcard-paths | fixed | P1 | renderPatchProposalPacket does not reject wildcard/glob paths in files, despite the runbook deny-list naming "Wildcard paths" explicitly | commit pending: explicit /[*?\[\]]/ guard in renderPatchProposalPacket + test |
| F004 | builder-packet-fails-on-missing-row-fields | fixed | P2 | Casts row.builder_commits / row.files / row.depends_on with `as unknown[]` and immediately calls .map; missing keys produce TypeError surfaced as exit 70 instead of a structured PacketRenderError | commit pending: requireRowArray helper throws typed PacketRenderError("malformed-ledger-row") |
| F005 | proposer-summary-not-stripped-of-fix-prose | fixed | P2 | Proposer's final_finding_row.evidence is stripped of fix prose but final_finding_row.summary forwards the full unstripped text; stripping is partially defeated | commit pending: strip applied to BOTH summary and evidence + test seeds "recommended fix: REWRITE-BAIT" marker |
| F006 | builder-batch-context-result-unused | fixed | P3 | renderBuilderPacket calls readLedgerBatchContext purely for side-effect validation and discards the result with `void batchContext`; cost is real (shells to git) | commit pending: removed; replaced with rationale comment |
| F007 | builder-issue-number-defaults-to-zero | fixed | P3 | readFrontmatterIssueNumber returns 0 when issue_number missing/non-numeric; Builder packet emits issue_number: 0 instead of failing fast | commit pending: throws PacketRenderError("missing-issue-number" / "invalid-issue-number"); test added |
| F008 | packet-response-shape-undocumented-in-help | fixed | P2 | HELP_DATA lists packet_roles and packet_flags but never declares the success-envelope data shape (role/packet/packet_markdown/dispatch_evidence) | commit pending: HELP_DATA.packet_response_shape added |
| F009 | packet-required-flags-not-declarative-in-help | fixed | P2 | HELP_DATA.packet_flags embeds required-vs-optional info inside prose; agents cannot programmatically enumerate required flags per role | commit pending: HELP_DATA.packet_role_flags structured matrix (required / optional / repeatable / enum) per role |
| F010 | packet-roles-not-exposed-as-contract-slice | fixed | P3 | packet_roles enumerated in HELP_DATA but not a contract subcommand slice; breaks symmetry with other agent-discoverable enums | commit pending: packet_roles added to CONTRACT_SLICES + CONTRACT_SLICE_VALUES |
| F011 | dispatch-evidence-shape-coverage-thin | fixed | P2 | Only role + cli_route_id asserted; timestamp / target_id / loaded_references / loaded_templates not contract-pinned despite U6 dependency | commit pending: cli.test.ts asserts all six DispatchEvidence fields incl. ISO-8601 timestamp; packets.test.ts asserts dispatch_evidence per role |
| F012 | patch-proposal-patch-batches-fixed-tuple-shape | closed | P3 | patch_batches is typed as a single-element tuple with inline ac_mapping: [] literal; future widening is a breaking type change | not-in-u5-scope: U5 deliberately encodes the one-patch-per-proposal invariant per the runbook patch-proposal contract. Widening is a U6+ design call when the helper validation rule changes. |
| F013 | packet-markdown-field-semantics-undocumented | fixed | P3 | Both `packet` data and `packet_markdown` are emitted; relationship between the two is undocumented in HELP_DATA | commit pending: HELP_DATA.packet_response_shape.packet_markdown documents the relationship |
| F014 | references-docs-restate-role-boundaries | fixed | P2 | findings-and-validators.md "Packet rendering contract" section restates deny-list invariants already owned by templates; runbook says "do NOT restate role boundaries" | commit pending: trimmed to CLI invocations + pointer to templates for invariants |
| F015 | builder-prior-evidence-deny-list-rubber-stamped | fixed | P0 | Builder deny-list test for rich prior evidence asserts absence without seeding bait; fixture has zero builder_attempts rows | commit pending: bait tokens (BAIT-IMPL-STEP-leak, BAIT-TEST-RUN-leak, BAIT-VALIDATOR-FOCUS-leak) seeded in unrelated-batch notes |
| F016 | proposer-summary-fix-prose-untested | fixed | P0 | stripFixProse marker-found branch is dead from test POV; no fixture seeds a summary with "fix:" / "recommended fix:" / "patch:" markers | commit pending: F005/F016 test seeds "recommended fix: REWRITE-BAIT-DO-NOT-LEAK" and asserts both summary and evidence are truncated |
| F017 | validator-full-ledger-bait-missing | fixed | P0 | No Validator test seeds whole-ledger bait (frontmatter scalars, plan body, unrelated batches) and asserts those tokens are absent | commit pending: F017 test seeds BAIT-UNRELATED-* tokens across unrelated batch + notes and asserts none leak |
| F018 | patch-proposal-determinism-untested | fixed | P1 | R8 determinism only tested for Builder packet; Proposer/Validator/patch-proposal/ce-plan unproven | commit pending: dedicated "R8 determinism per role" describe block covers all five roles |
| F019 | validator-stage-3-pseudo-batch-untested | fixed | P1 | renderValidatorPacket has a stage-3 branch synthesizing virtual batch; only "final" branch tested | commit pending: F019 test pins batch_id: stage-3 + batch_goal "review the candidate batch DAG for contract violations" |
| F020 | cli-packet-roles-3-of-5-uncovered | fixed | P2 | proposer / validator / patch-proposal have no CLI-level happy-path test through invoke() | commit pending: cli.test.ts adds invoke-level happy-path tests for all three roles |
| F021 | dispatch-evidence-assertion-gap-validator-patch-proposal | fixed | P2 | Validator and patch-proposal tests never assert dispatch_evidence at all | commit pending: "Dispatch evidence per role" describe block in packets.test.ts pins both |
| F022 | builder-unrelated-batch-bait-too-generic | fixed | P2 | Unrelated-batch deny test uses literal "other" as bait — would false-positive on common English | commit pending: bait token renamed to bait-batch-do-not-leak / BAIT-OTHER-FILE.ts |
| F023 | proposer-finding-not-open-branch-untested | fixed | P3 | finding-not-open guard branch is uncovered by tests | commit pending: F023 test exercises a finding with status "fixed" |
| F024 | cli-attempt-type-validation-untested | fixed | P3 | --attempt-type invalid-value branch is uncovered (e.g. `--attempt-type wizard`) | commit pending: F024 cli test passes --attempt-type wizard, asserts missing-packet-flag |
| F025 | determinism-excludes-dispatch-evidence | fixed | P3 | R8 determinism test does not assert dispatch_evidence is structurally equal (modulo timestamp) | commit pending: per-role determinism tests assert JSON.stringify(dispatch_evidence) equality (timestamp is frozen) |
| F026 | unquoted-yaml-enum-fields-allow-injection | fixed | P1 | Severity/status/execution_mode rendered unquoted in YAML output without enum validation; YAML control chars in source could break parsing | commit pending: enum-validate severity / status / execution_mode in parsers; throw PacketRenderError on mismatch; test asserts rejection |
| F027 | notes-substring-match-allows-cross-batch-leak-and-injection | superseded | P1 | Duplicate of F001 from security lens; same root cause + same fix | superseded-by-F001 |
| F028 | ledger-path-accepted-without-threat-model | fixed | P2 | --ledger accepts arbitrary filesystem paths with no allowlist; no documented threat model | commit pending: explicit F028 threat-model docstring on readLedgerSource declares CLI trusts the Orchestrator caller |
| F029 | findings-row-fails-open-on-pipe-in-summary | fixed | P2 | Finding summary containing `\|` either trips malformed-findings-row (DoS) or silently corrupts the resolution column | commit pending: F029 test pins fail-closed behavior (8-column check throws); enum validation closes the silent-corruption sub-case |
| F030 | validator-suggested-validator-focus-forwarded-verbatim-without-fact-framing | fixed | P2 | Builder-authored suggested_validator_focus strings forwarded verbatim with no framing marking them as Builder claims | commit pending: validator-envelope.md template prose declares "Builder evidence is Builder-asserted, not Orchestrator-authorized" |
| F031 | missing-tests-for-hostile-yaml-and-table-inputs | fixed | P3 | No describe("hostile inputs") block — no fixtures for pipes in summaries, framing-tag literals, YAML control sequences | commit pending: "Hostile inputs and contract guards" describe block covers wildcard, invalid severity, invalid status, pipe-in-summary |
| F032 | builder-packet-confirmation-state-read-not-validated | closed | P2 | Builder renderer does not check confirmation_state from snapshot before rendering — Builder could receive packet for pending/stale batch | deferred-to-u7-router: confirmation_state gating is the Orchestrator/hot-router's responsibility per ADR 0001 (orchestration vs mechanic split). Renderer rationale captured in lib/packets.ts F032 comment. |
| F033 | proposer-evidence-field-shape-misleading | fixed | P2 | Proposer's final_finding_row.evidence is stripped summary; runbook says evidence should be "reviewer evidence" but ledger has no separate evidence column | commit pending: F005 fix also addresses this — evidence and summary are both stripped from the same ledger source with a comment documenting that the ## Findings table has no separate evidence column |
| F034 | packet-cli-test-missing-proposer-deny-list-assertions | fixed | P2 | No CLI-level deny-list assertion for proposer/validator/patch-proposal — only Builder happy-path is exercised at envelope boundary | commit pending: proposer CLI test asserts envelope.packet does not contain commit_sha / builder_commits |
| F035 | builder-commits-missing-throws-typeerror | fixed | P2 | renderBuilderPacket still casts row.builder_commits as unknown[] and calls .map directly; missing key produces TypeError -> exit 70 (same bug class as F004 closed for other fields) | commit pending: routed through requireRowArray helper |
| F036 | strip-fix-prose-substring-match-too-greedy | fixed | P2 | stripFixProse indexOf('fix:') matches inside 'prefix:', 'suffix:', etc; can silently truncate summary/evidence to empty string | commit pending: word-boundary regex with case-insensitive match; slice from boundary char |
| F037 | ac-mapping-non-numeric-silently-yields-nan | fixed | P3 | ac_mapping.map(parseInt) silently emits NaN -> rendered as literal "NaN" YAML scalar instead of typed PacketRenderError | commit pending: typed PacketRenderError("invalid-ac-mapping") on non-finite parse |
| F038 | builder-dispatch-evidence-missing-loaded-references-assert | fixed | P2 | builder dispatch_evidence test asserts 5 of 6 fields; loaded_references is not unit-pinned | commit pending: assertion added; test also converted to async/await per T001 |
| F039 | proposer-dispatch-evidence-asserts-3-of-6 | fixed | P2 | proposer dispatch_evidence unit test asserts only role/target_id/cli_route_id; timestamp/loaded_references/loaded_templates not pinned | commit pending: all six fields asserted |
| F040 | ce-plan-dispatch-evidence-asserts-2-of-6 | fixed | P2 | ce-plan dispatch_evidence asserts only role + cli_route_id; the other four fields are unpinned | commit pending: all six fields asserted |
