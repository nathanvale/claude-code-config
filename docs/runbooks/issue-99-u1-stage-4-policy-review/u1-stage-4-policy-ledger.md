# U1 Stage 4 dispatch policy - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 1 | inline-eligibility-obvious-clause-dropped | fixed | low | Plan R1 and the canonical bound list (`stage-4-batch-loop.md:73` "obvious and low-risk") include an `obvious` clause, but the four compact restatements (ADR 0001:8, SKILL.md:359, v1 README:90, v1 issue-to-pr.md:59) skipped from the file-count bound straight to "low-risk", silently dropping `obvious`. | Fixed: inserted `obvious,` after the file-count clause in all four compact sites (ADR 0001, SKILL.md, v1 README, v1 issue-to-pr.md) so every restatement carries the full R1 bound. |
| 2 | v1-inner-loop-readiness-scoped-to-builder-dispatch | fixed | low | `runbooks/issue-to-pr/issue-to-pr.md:1070` read "Before every Builder dispatch ... verify host Builder readiness", but ADR 0001 moved readiness to "before every Stage 4 implementation attempt (Builder dispatch or Orchestrator-inline)". A literal read skipped the readiness gate before inline `change_first` work. | Fixed: reworded to "Before every Stage 4 implementation attempt (Builder dispatch or bounded Orchestrator-inline), including resumed implementation and repair attempts, verify host Builder readiness". Matches outer-loop step 3 and host-adapters wording. |
| 3 | v1-inline-evidence-term-drift | fixed | low | `runbooks/issue-to-pr/issue-to-pr.md:1023` said "malformed inline attempt evidence" while the canonical term is "malformed Orchestrator-inline evidence" (ADR 0001:19; same file already uses canonical term at line 750). | Fixed: changed to "malformed Orchestrator-inline evidence" to match ADR 0001 and findings-and-validators verbatim. |
| 4 | skill-bounded-inline-term-drift | fixed | low | `skills/issue-to-pr/SKILL.md:409` said "bounded inline" while line 167 of the same file and all other surfaces say "bounded Orchestrator-inline". | Fixed: changed L409 to "bounded Orchestrator-inline". |
| 5 | v1-source-anchor-line-ranges-stale | fixed | low | U1's prose additions to `runbooks/issue-to-pr/issue-to-pr.md` shifted headings down, so the descriptive "v1 L<start>-<end>" hint citations in `builder-dispatch.md` and `stage-4-batch-loop.md` pointed at wrong lines (e.g. role boundaries L43-65 -> actual L44, dispatch contract L67-265 -> actual L75, inner-loop L1017-1035 -> actual L1047). Navigational hints, not clickable links. | Fixed: re-derived accurate ranges against the post-U1 v1 file. builder-dispatch.md -> L13-22 / L44-74 / L75-285 / L1087-1140 + README L35-165; stage-4-batch-loop.md -> L703-781 (outer loop) / L1047-1064 (inner-loop diagram) / L833-886 (decision tree). |
| 6 | inline-bound-list-hand-copied-six-sites | closed | low | The inline-eligibility bound list appears in several sites. | Closed (by-design): R8 intentionally requires each surface (canonical `stage-4-batch-loop.md`, ADR 0001, SKILL.md, v1 README L90, v1 issue-to-pr.md L59) to independently describe the policy for standalone readability. The README's second mention (L101 "Builder dispatch overview") already says "every inline bound above holds" - a pointer, not a sixth verbatim copy. The one real drift (`obvious`, row 1) is fixed. Converting deliberate restatements to pointers would over-abstract against R8 and the anti-list ("no policy redesign"). |
| 7 | host-adapters-u3-edits-on-branch | closed | low | `runbooks/issue-to-pr-v2/references/host-adapters.md` has uncommitted edits, but it is a U3 file, not U1. | Closed (out-of-u1-scope): the file is U3's surface per the plan. Flagged for separate U3 staging; not a U1 review defect. |
| 8 | findings-and-validators-u6-edits-on-branch | closed | low | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` has uncommitted edits, but it is a U6 file, not U1. | Closed (out-of-u1-scope): the file is U6's surface per the plan. Flagged for separate U6 staging. |
| 9 | stage-4-policy-drift-test-not-u1-artifact | closed | low | `runbooks/issue-to-pr-v2/stage-4-policy-drift.test.ts` is untracked and not in U1's file list; the plan assigns drift-test work to U7's `contract-drift.test.ts`. | Closed (out-of-u1-scope): not a U1 artifact. Flagged for U7 (consider folding into `contract-drift.test.ts` per the plan's declared test surface). |
| 10 | settings-json-unrelated | closed | low | `settings.json` has an unrelated workspace-config change on the branch. | Closed (out-of-u1-scope): unrelated to Stage 4 policy; flagged for separate staging. |
| 11 | u2-plan-reference-in-batch-loop | fixed | low | `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` committed text said "moved here per the U2 plan" (a `\bU[0-9]\b` unit-label leak into shared prose). | Fixed (already in working tree): now reads "moved here during the v2 reference split". Verified no `\bU[0-9]\b` unit-label leak remains in the shared docs. |
| 12 | adr-0001-uncommitted-wording-refinements | fixed | low | `docs/adr/0001-stage-4-context-isolation.md` had in-scope uncommitted lexical tightening (quantified `<=2 touched files`, evidence-source phrasing). | Fixed (in-scope, intended final wording): verified the quantified bound and evidence-source phrasing are present and correct; row 1 added the `obvious` clause on top. |
| 13 | inline-bound-quantifier-small-vs-2-files | fixed | low | ADR 0001 and v1 role boundaries earlier said the vague "small"; working-tree edits tightened both to "<=2 touched files". | Fixed (already in working tree): verified no vague "small" quantifier remains; all surfaces use "<=2 touched files". |
| 14 | patch-batch-first-attempt-inline-loophole | fixed | low | Adversarial pass (P1): `stage-4-batch-loop.md:295-296` and v1 `issue-to-pr.md:890-891` said "the Stage 4 **implementation path** owns one attempt against that confirmed contract. Repairs remain Builder-only." A final-review P0/P1 patch-batch is `change_first` and ≤2 files by construction, so its **first** attempt is not a per-batch "repair" and a literal reader could route it Orchestrator-inline, contradicting ADR 0001's "open P0/P1 after any committed attempt routes to Builder-only repair, never inline" for the highest-stakes findings. | Fixed: tightened both in-scope sentences to "a confirmed batch that remediates an open final-review P0/P1; it is Builder-only on every attempt, never Orchestrator-inline, because it carries an open P0/P1 forward." Aligns the patch-batch path with ADR 0001. Low risk: wording aligned to already-decided policy, not a redesign. |
| 15 | patch-batch-closure-evidence-builder-vs-implementation | fixed | low | Adversarial pass: in-scope v1 `issue-to-pr.md:1328` resolution table said a patch-batch closes via "reachable **implementation** commits" while the canonical (out-of-scope U6) `findings-and-validators.md:208` says "reachable **Builder** commits". After row 14 made patch-batches Builder-only, the in-scope table drifted from the policy. | Fixed: changed the patch-batch clause in v1 `issue-to-pr.md:1329` to "reachable Builder commits" (kept the normal-batch clause as "implementation commit", which correctly allows inline OR Builder for ordinary batches). The U6 `findings-and-validators.md` side is out of strict-U1 scope but already uses the correct "Builder commits" wording, so no cross-file conflict remains. |
| 16 | patch-batch-not-in-canonical-builder-required-list | fixed | low | Adversarial pass 4 (P1): the row-14 fix put "Builder-only on every attempt" only in the Stage 5 patch-batch decision-tree narrative, but the **dispatch decision** is governed by the canonical "Builder dispatch is required for" list (`stage-4-batch-loop.md:59-66`, the single source of truth per builder-dispatch.md). That list had no patch-batch member, and a patch-batch's *initial* attempt is not a per-batch "repair", so a `change_first` docs-only patch-batch's first commit was still inline-eligible by the canonical rule the Orchestrator actually reads. | Fixed: added "every attempt on a patch-batch (`id: patch-NNN`), including its initial implementation attempt ... the `change_first` inline exemption never applies" to the canonical Builder-required list; added "the batch is not a patch-batch" as an inline-eligibility bound; mirrored the patch-batch clause into the three other dispatch-policy restatements (builder-dispatch.md "Applies To", v1 issue-to-pr.md "Applies to", ADR 0001 consequence). Now the dispatch rule the Orchestrator consults at dispatch time states Builder-only for patch-batches. Low risk: states ADR 0001's already-settled "open P0/P1 -> never inline" intent where it is enforced. |
| 17 | skill-dispatch-policy-omits-patch-batch-carveout | fixed | low | Adversarial pass 5 (P1): the row-16 fix added the patch-batch clause to 4 dispatch-policy surfaces but missed `skills/issue-to-pr/SKILL.md` (L357-363 dispatch policy + L389 `implementation-attempt-inline` subroute). SKILL.md is the **active control plane** and restates an abridged inline-eligibility bound list with no patch-batch exclusion, so an Orchestrator routing from SKILL.md would still send a `change_first` patch-batch initial attempt inline - an R8 cross-document disagreement on the most authoritative dispatch-time surface. (The correctness reviewer read this as by-design delegation; adjudicated as a real finding because SKILL.md's self-contained MAY-stay-inline rule permits the route the other 4 surfaces forbid, and SKILL.md is the control plane.) | Fixed: added "and every attempt on a patch-batch (`id: patch-NNN`, ... never inline-eligible) MUST dispatch Builder" to SKILL.md's dispatch policy, and "never for a patch-batch ... Builder-only on every attempt" to the `implementation-attempt-inline` subroute. All 5 dispatch-authority surfaces now agree. Low risk: control-plane wording aligned to the already-fixed policy. |
| 18 | v1-readme-bound-list-omits-patch-batch | fixed | low | Adversarial + coherence pass 6 (P1): the row-16/17 mirrors missed `runbooks/issue-to-pr/README.md` - its TWO inline-eligibility restatements ("Execution mode ownership" L88-95 and "Builder dispatch overview" L99-105) omitted the patch-batch exclusion, and the README never stated patch-batches are Builder-only. The README is the human entry point and its own cross-reference points to the also-incomplete overview, so a README-routed reader would conclude a `change_first` patch-batch is inline-eligible. | Fixed: added "the batch is not a patch-batch" to the L88-95 bound list and "every attempt on a patch-batch (`id: patch-NNN`, ... never inline-eligible), must dispatch Builder"; added the patch-batch member + dispatch trigger to the "Builder dispatch overview". Both README restatements now match the other surfaces. Self-critique: I should have caught these two restatements in pass 5 when mirroring the clause; the README was in scope the whole time. |
| 19 | v2-router-invariant-omits-patch-batch | fixed | low | Adversarial pass 6 (P2 residual): v2 hot-router core invariant #4 (`runbooks/issue-to-pr-v2/issue-to-pr.md:34-38`) enumerated the Builder-required set ("tdd, proof_first, and every repair") without the patch-batch member. Lower severity (v2 defers detailed patch-batch handling to the reference), but it is a same-shape enumeration so omitting the member is exactly the partial-restatement drift this seam guards. | Fixed: added "and every attempt on a patch-batch (`id: patch-NNN`, never inline-eligible)" to invariant #4 so every enumerating surface agrees. Low risk: summary wording aligned to the canonical set. |
| 20 | remaining-bound-lists-omit-patch-batch | fixed | low | Pass 6 definitive enumeration: two more bound-list restatements were missed by the row-16/17/18 mirrors - the v1 `issue-to-pr.md` role-boundaries inline-exemption (L59-63, distinct from its "Applies to" block) and the ADR 0001 inline-eligibility parenthetical (L8). Both omitted the "not a patch-batch" bound. | Fixed: added "the batch is not a patch-batch" to both eligibility bound lists, and the patch-batch member to the v1 role-boundaries Builder-required sentence. After this, every inline-eligibility bound list (canonical, v1 role-boundaries, v1 README, ADR 0001) and every Builder-required enumeration (6 surfaces) carries the patch-batch carve-out. Self-critique: the per-restatement enumeration sweep should have been the Pass-5 method instead of fixing the loophole surface-by-surface as reviewers surfaced each. |
| 21 | readme-fix-protocol-omits-patch-batch | fixed | low | Adversarial pass 7 (P2): a THIRD v1 README restatement - the "Fix protocol (shared)" inner-loop step 1 (L298-301) - said "bounded Orchestrator-inline only for eligible `change_first`" without the explicit patch-batch carve-out. Not a hard contradiction (the word "eligible" chains to the L90 definition that now excludes patch-batches) but the one remaining asymmetric, implicit-deference restatement; read in isolation a `change_first` patch-batch looks inline-eligible. | Fixed: added "any patch-batch (`id: patch-NNN`, never inline-eligible)" to the Builder-dispatch list and "on a non-patch-batch" to the inline clause. This was the last restatement of the inline rule; all are now self-contained and symmetric. |
| 22 | readme-execution-mode-list-grammar-break | fixed | low | Adversarial pass 8 (P3): my own pass-6 edit to README L88-97 left a broken two-item list ("...exception); a repair attempt after any open P0/P1, and every attempt on a patch-batch (...), must dispatch Builder") - mismatched determiners and a stray comma before "and". Meaning unambiguous but awkward. | Fixed: restructured to "Builder dispatch is mandatory regardless of mode for a repair attempt after any open P0/P1 and for every attempt on a patch-batch (...)". Clean parallel construction. Low risk: self-introduced prose cleanup, no meaning change. |
| 23 | header-v1-source-anchors-redrifted | fixed | low | Adversarial pass 8 (P3): the cumulative patch-batch edits added lines to v1 `issue-to-pr.md`, re-shifting headings so the `**v1 source anchors:**` header citations in the two v2 references (last set in row 5) drifted again (Builder dispatch contract L75->L78, inner loop L1047->L1053, decision tree L833->L838, Builder rules L1087->L1093). | Fixed: re-derived the header citations to current positions - builder-dispatch.md L78-290 / L1093-1146 / README L35-166; stage-4-batch-loop.md L708-786 / L1053-1070 / L838-901 / L877-884. |
| 24 | embedded-heading-slug-line-numbers-stale | closed | low | Pass 8 discovery: both v2 references carry many embedded `(v1 L...)` line numbers IN HEADING TEXT/slugs (e.g. `## Outer loop (v1 L692-753)`, `## Inner loop (v1 L1017-1035)`, 11 in builder-dispatch.md). Several were already stale on `main` before U1 (the inner-loop `L1017-1035` is the pre-U1 position; smallest-contract is actually at L910 vs slug L872-879). | Closed (out-of-u1-scope, pre-existing debt): U1 did not introduce this line-number-in-heading pattern, and re-deriving ~15 embedded slugs is scope creep beyond U1's policy review (the scope-discipline boundary). The adversarial reviewer itself rated this P3 maintainability debt "out of adversarial scope". Flagged for a dedicated maintainability pass or a U7 drift check that replaces line-number slugs with heading-name anchors. The header summary citations (row 23) ARE fixed, so U1 leaves the anchor surface better than `main`, not worse. |
| 25 | inline-consecutive-counter-not-durably-asserted | fixed | low | Adversarial pass 10 (P2, conf 55, fresh angle): the third-consecutive-inline bound (`stage-4-batch-loop.md:88-92`) depends on counting "consecutive committed inline attempts with no intervening Builder dispatch", but the U1 prose described the inline audit lane without asserting the count is reconstructable from durable ledger state on a resumed turn - a tension with Core Invariant 1 (durable state beats transcript). The reviewer's own fix said to assert durability in policy prose WITHOUT specifying schema (schema = downstream unit). | Split by scope. **In-scope (fixed):** added one schema-free sentence to the SoT bound stating the count "is derived from durable ledger state, not transcript memory, so it is recoverable on a resumed turn" and that "the exact field shape is owned by the ledger contract, not this policy" - this satisfies Core Invariant 1 at the policy layer, U1's job. **Out-of-scope (belongs-to-later-unit):** the actual ordering-key/field-schema realization that makes the count mechanically reconstructable is U4 (`orchestrator_inline_attempts` shape) + U5 (attempt counting), which the plan explicitly defers and this strict-U1 seam scopes out. |
