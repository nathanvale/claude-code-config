# Placement and ADRs - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 001 | adr-citations-resolve-to-correct-clause | closed | low | P-none. All six `false-positive: ADR-*` closures in the four prior ledgers cite ADR-0002 and resolve to a correct, accurate clause. INV F002 (`ac_confirmation_status: confirmed` behind CLI) → ADR-0002 L28 "runtime contract values such as allowed statuses... live in code". INV F006 (`decompose.ts --assert-no-open-p0p1`) → L27 "validation... belongs behind a CLI or script". INV F013 (blocking_gates discriminated-union in lib/route.ts/lib/contract.ts) → L28 "schema keys... live in code; TypeScript types alone are not enough". INV F015 (/ce-code-review persona naming) → L31 "references own detailed or rarely needed explanation". INV F018 (`installed_artifact_presence` struct) → L28 (same class as F013). STOP F008 (hatch names in `findings-and-validators.md`) → L31. No finding was closed `false-positive: ADR-0001` in any ledger, so there is no ADR-0001 citation to validate. | verified-present |
| 002 | adr-0002-literals-absent-from-skill | closed | low | P-none. The six deterministic literals the ADR-0002 closures keep out of the skill are all absent from SKILL.md (grep confirmed): `ac_confirmation_status`, `--assert-no-open-p0p1`, `BLOCKING_GATE_FIELD_NAMES`, `kind:"route_id"/"field"` discriminant, hatch names (`same-signature-twice` / `finding-count-rises` / `tautological-test`), and the `installed_artifact_presence` struct fields (`cli_ts` / `lib_dir`). The load-bearing behavioral claim for each is preserved in prose (Stage 1 exit L245-247, success_criteria L408-414, pre_route_gates L157-162), and L104-107 / L194-196 explicitly forbid restating the schema/union. Closures are factually accurate, not just ADR-mapped. | verified-present |
| 003 | skill-frontmatter-names-control-plane | closed | low | P-none (point 2). SKILL.md frontmatter `description` (L3) names it "the control plane"; `<objective>` L12-15 states "This skill is the host-neutral control plane." Host-neutral control-plane framing present in frontmatter and reinforced in the objective. | verified-present |
| 004 | hot-router-header-cedes-authority | closed | low | P-none (point 3, P0 check). issue-to-pr.md title (L1) is "Issue to PR (v2 hot-router support file)"; header L15-17 states "The public host-neutral control plane lives at `skills/issue-to-pr/SKILL.md`. This file remains the installed v2 hot-router support artifact and Claude Code compatibility reference." Title + opening unambiguously cede public-control-plane authority; a fresh reader of the header cannot recover the pre-migration authoritative-router model. Strong, not weak framing — no P0. | verified-present |
| 005 | hot-router-start-every-turn-stale-source-of-truth | closed | low | P1-class wording, dispositioned out-of-scope by user. issue-to-pr.md `## Start every turn` step 1 (L82-83) still reads "Re-read this hot file... the file is the source of truth" with no per-section back-pointer to the skill, and `## ADR pointers` (L435-442) / `## Stop-and-ask checklist` (L462-463, "a P0 finding against this file") restate workflow policy in first-person-router voice. Under point 3 this is the P1 pattern (later section restating policy without a skill back-pointer). It is NOT a skill parity defect (SKILL.md correctly owns orchestration; INV/reference/stage/stop seams all converged). The hot router is read-only in this audit (README L124, L273-274) and its thinning/freezing is explicitly a future runbook's job (README L74-75). User decision (2026-05-24): note as out-of-scope cosmetic wart owned by the deprecation runbook, do not fix here. | out-of-scope: covered-by-future-deprecation-runbook |
| 006 | readme-is-finder-not-policy-manual | closed | low | P-none (point 4). runbooks/issue-to-pr-v2/README.md lists the skill control plane FIRST in the file map (L60-61, item 1 "the public skill control plane") and names issue-to-pr.md as item 2 "the v2 hot-router support file" (L62-64). It duplicates no workflow policy: L13 "the linked artifact owns the prose", and "What this area deliberately does not do" (L154+) routes every topic to its owning artifact. Finder contract intact. | verified-present |
| 007 | codex-first-class-goal-loop-scoped | closed | low | P-none (point 5). All four `/goal`/`/loop` mentions in SKILL.md sit inside `<host_adapters>`: L70 under the **Codex** subhead is a negative scoping ("Do not depend on `/goal` or `/loop`"), L77/L81/L82 under the **Claude Code** subhead. Codex gets its own first-class adapter block (L63-71) with direct-drive instructions. No `/goal`/`/loop` leak into `<orchestration_loop>`, `<route_catalog>`, `<stage_shells>`, or other policy sections. | verified-present |
| 008 | no-claim-closed-historical-only | closed | low | P-none (point 6). grep across all four prior ledgers found zero `historical-only` closures. No claim that should live in the skill was demoted to hot-router-only. | verified-present |
| 009 | no-hot-router-only-claim-elevated | closed | low | P-none (point 7, inverse of 002). The skill absorbed none of the hot-router-only material: deterministic literals absent (see 002); the legacy `/loop 5` prompt body (issue-to-pr.md L476-484), v1-vs-v2 skew prose, and the internal R3/R8/R10/R11 ADR-pointer rules (L443-456) all stayed in the hot router. Nothing that should stay deterministic/historical was elevated into prose. | verified-present |
| 010 | no-context-md-edit-no-new-adr | closed | low | P-none (point 8). `docs/adr/` contains only 0001 and 0002 (no new ADR). `git status` shows no CONTEXT.md or `docs/adr/` changes; no CONTEXT.md exists in the audit area or the v2 tree. Seam surfaced no durable new term requiring promotion. | verified-present |
| 011 | convergence-bookkeeping | closed | low | Convergence bookkeeping (not a defect). No fix landed in this seam (005's P1-class wording is dispositioned out-of-scope, owned by the future deprecation runbook), so after the initial inventory two consecutive independent clean passes ran from different angles per the README Convergence protocol. Pass 2 (cross-link graph + reverse-direction contract): every cross-link in README/skill/hot-router resolves on disk; the three-way control-plane claims are mutually consistent (README calls skill "control plane" ↔ SKILL.md L3/L12-15; README calls issue-to-pr.md "support file" ↔ its L1 title; skill never over-claims hot-router authority). Zero new findings. Pass 3 (re-derive false-positive inventory from scratch + ADR-clause freshness): grep re-derived exactly 6 `false-positive: ADR-0002` + 1 `verified-present`, matching Pass 1 with none missed and none citing ADR-0001; ADR-0002 anchors L27/L28/L31 re-read and still back each closure verbatim. Zero new findings. Seam converged. | converged: 2 consecutive clean passes (2 cross-link graph, 3 inventory re-derive + ADR freshness) |
