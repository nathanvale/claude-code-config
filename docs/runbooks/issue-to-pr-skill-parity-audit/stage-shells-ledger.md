# Stage shells - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 001 | stage-2-exit-branch-rename-missing | fixed | high | Runbook Stage 2 exit condition (issue-to-pr.md L279) lists "branch renamed" as a load-bearing exit clause alongside `plan_path` set / plan exists / tree clean, mirrored in the Stage 2 reference exit (`references/stage-2-plan.md` L49-50). The skill Stage 2 exit (SKILL.md L259) lists `plan_path` set, plan exists, tree clean, routes to `decompose` but omits branch-renamed. The branch name is a host-side invariant not derivable from the `cli.ts state` envelope, so a control plane missing this clause could route to `decompose` on a still-`-pending` branch. Exit condition is looser than the hot router, not tighter. | restored "feature branch is renamed off its `-pending` placeholder" to the Stage 2 exit condition (SKILL.md `<stage_shells>`), keeping deep `feat/...-<slug>` mechanics in `references/stage-2-plan.md` per ADR-0002 |
| 002 | stage-1-stop-ac-ambiguity-added | closed | low | Skill Stage 1 stop conditions (SKILL.md L247-248) add "unresolved AC ambiguity", which is not a named Stage 1 stop in the runbook. The Stage 1 reference resolves AC ambiguity through the `y`/`edit`/`abort` confirm loop (`references/stage-1-pick-issue.md` L66-79); an un-confirmable AC terminates via the already-carried "user abort" path. The added phrase is a non-contradicting generalization, subsumed by abort, and drops no runbook stop. Not load-bearing, not a parity defect. | out-of-scope: covered-by-stop-conditions |
