# Stage shells - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 001 | stage-2-exit-branch-rename-missing | fixed | high | Runbook Stage 2 exit condition (issue-to-pr.md L279) lists "branch renamed" as a load-bearing exit clause alongside `plan_path` set / plan exists / tree clean, mirrored in the Stage 2 reference exit (`references/stage-2-plan.md` L49-50). The skill Stage 2 exit (SKILL.md L259) lists `plan_path` set, plan exists, tree clean, routes to `decompose` but omits branch-renamed. The branch name is a host-side invariant not derivable from the `cli.ts state` envelope, so a control plane missing this clause could route to `decompose` on a still-`-pending` branch. Exit condition is looser than the hot router, not tighter. | restored "feature branch is renamed off its `-pending` placeholder" to the Stage 2 exit condition (SKILL.md `<stage_shells>`), keeping deep `feat/...-<slug>` mechanics in `references/stage-2-plan.md` per ADR-0002 |
| 002 | stage-1-stop-ac-ambiguity-added | closed | low | Skill Stage 1 stop conditions (SKILL.md L247-248) add "unresolved AC ambiguity", which is not a named Stage 1 stop in the runbook. The Stage 1 reference resolves AC ambiguity through the `y`/`edit`/`abort` confirm loop (`references/stage-1-pick-issue.md` L66-79); an un-confirmable AC terminates via the already-carried "user abort" path. The added phrase is a non-contradicting generalization, subsumed by abort, and drops no runbook stop. Not load-bearing, not a parity defect. | out-of-scope: covered-by-stop-conditions |
| 003 | stage-1-exit-feature-branch-missing | fixed | high | Runbook Stage 1 exit condition (issue-to-pr.md L259) lists "feature branch in place" as a load-bearing exit clause alongside ledger-exists / AC-confirmed-and-committed / tree-clean / `route_id: plan`, mirrored in the Stage 1 reference exit (`references/stage-1-pick-issue.md` L125, "current branch is a feature branch"). The skill Stage 1 exit (SKILL.md L245-246) lists ledger exists, ACs confirmed and committed, tree clean, routes to `plan` but omits the feature-branch clause. Branch state is a host-side invariant not derivable from the `cli.ts state` envelope's `confirmation_state`/`route_id` axes, so a control plane could reach `route_id: plan` while still on the default branch and pass the skill's stated exit. Exit condition is looser than the hot router, not tighter. Same class as 001. | restored "the issue feature branch is in place" to the Stage 1 exit condition (SKILL.md `<stage_shells>`), keeping `feat/issue-{n}-pending` preflight mechanics in `references/stage-1-pick-issue.md` per ADR-0002 |
| 004 | stage-5-exit-tree-clean-missing | fixed | high | Runbook Stage 5 exit condition (issue-to-pr.md L355) lists "tree clean" as a load-bearing exit clause alongside `final_reviewed_at` set / every `batch_id: final` row terminal / `route_id: ship`, mirrored in the Stage 5 reference exit (`references/stage-5-final-review.md` L98, "working tree clean"). The skill Stage 5 exit (SKILL.md L325-326) lists final findings terminal, `final_reviewed_at` set and committed, routes to `ship` but omits the tree-clean clause. Tree state is a host-side invariant not in the `cli.ts state` route axes, so a control plane could set `final_reviewed_at`, see `route_id: ship`, and proceed with a dirty tree against the skill's stated exit (Stage 6 inputs catch it downstream, but the Stage 5 exit is looser than the hot router). The skill carries "tree clean" in every other stage exit (1-4, 6); its absence from Stage 5 alone is a parity gap. Same class as 001/003. | restored "the working tree is clean" to the Stage 5 exit condition (SKILL.md `<stage_shells>`) |
| 005 | convergence-bookkeeping | closed | low | Convergence bookkeeping (not a defect). After fixing 003/004, two consecutive independent clean passes ran from different angles per the README Convergence protocol. Pass A: exhaustive directive enumeration — every runbook stop-condition token and exit clause for Stages 1-6 has a home in the skill `<stage_shells>`/`<fail_stops>`; all 11 stage-shell reference paths resolve on disk. Pass B: ADR-0001 role boundaries (Stage 4 seven subroutes preserve Orchestrator/Builder/Validator/Proposer separation; Proposer Stage-5-only), Stage 4 one-visible-action-per-turn asserted (SKILL.md L284, L310), Stage 5 read-only invariant intact (L322-324 + `<review_loop>` L393-396), ADR-0002 no-inlining verified (no code fences, hatch names, ROUTE_IDs, or schema tokens in the shells). Zero new findings in both passes. Seam converged. | converged: 2 consecutive clean passes (A enumeration, B ADR-boundary) after 003/004 fix |
| 006 | convergence-bookkeeping-reentry | closed | low | Convergence bookkeeping (not a defect). Fresh `/goal` re-entry re-ran the protocol from pass one (no fixes landed since 005, so no counter reset). Pass A (field-by-field parity): every Stage 1-6 shell carries inputs, required references, one visible action, exit condition, and stop conditions matching the runbook or strictly tighter — Stage 1 exit feature-branch clause (SKILL.md L246-247), Stage 2 exit branch-renamed (L260-261), Stage 5 exit tree-clean (L327-328) all present; all 16 reference + template paths resolve on disk; Patch-batch playbook load-bearing claims (read-only, Proposer route, replan fail-stop) carried across `<stage_shells>` Stage 5 (L324-325), `<review_loop>` (L395-398), `<fail_stops>`. Pass B (exhaustive directive grep + ADR boundaries): no code fences / hatch literals / ROUTE_IDS / schema tuples inside `<stage_shells>` (L232-346); all 14 runbook stop-condition tokens map to a skill home; ADR-0001 four-role separation intact across the seven Stage 4 subroutes with one-visible-action asserted (L311). Zero new findings in both passes. Note: 005's line citations had drifted (one-visible-action now L311, Stage 5 read-only L322-325, review_loop L395-398); this row carries current citations. | converged: 2 consecutive clean passes on fresh /goal re-entry (A field parity, B directive-grep + ADR), citations refreshed |
