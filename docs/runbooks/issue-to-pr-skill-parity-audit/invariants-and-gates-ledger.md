# Invariants and gates - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 001 | inv1-durable-state-beats-memory | closed | low | P-none. INV1 present: skill `<durable_state_contract>` L89 ("Durable ledger state beats transcript memory") + L99-100 ("first non-read operation on every resumed turn") + `<orchestration_loop>` L119-120. Matches runbook L23-25. | verified-present |
| 002 | inv2-ac-confirmed-before-plan | closed | low | P-none. INV2 behavioral gate present: Stage 1 exit L243-245 ("ACs are confirmed and committed... routes to plan") + Stage 2 input L250. Literal `ac_confirmation_status: confirmed` is a deterministic field, correctly behind CLI. Matches runbook L26-28. | false-positive: ADR-0002 |
| 003 | inv3-batch-contract-confirmed | closed | low | P-none. INV3 present: Stage 3 L266-273 ("digest-backed batch contract for confirmation... coverage and digests are confirmed"); AC-mapping=coverage, execution-modes covered by "missing batch contract field" stop. Matches runbook L29-31. | verified-present |
| 004 | inv4-builder-edits-confirmed-files | closed | low | P-none. INV4 verbatim: skill L296 ("Builder edits only the confirmed `batch.files`") + L310-311 + `<review_loop>` L380. Matches runbook L32-33. | verified-present |
| 005 | inv5-validators-own-findings | closed | low | P-none. INV5 present: skill L297-299 + L310 ("Validators own correctness findings; the Orchestrator records and normalizes them") + `<review_loop>` L378-379. Matches runbook L34-36. | verified-present |
| 006 | inv6-open-p0p1-blocks-convergence | closed | low | P-none. INV6 behavioral claim present at both convergence and final review: skill L284, `<review_loop>` L380+L384, `<success_criteria>` L399. Literal `decompose.ts --assert-no-open-p0p1` is a CLI mechanic, correctly behind helper. Matches runbook L37-39. | false-positive: ADR-0002 |
| 007 | inv7-clean-tree-every-transition | fixed | low | P1. INV7 (runbook L40-41) is a UNIVERSAL invariant: clean tree before ANY state-changing transition. Skill previously stated clean-tree only as per-stage exit conditions; Stage 3 and Stage 4 exits omitted it and there was no universal statement. FIX: added universal invariant to `<orchestration_loop>` step 9 (L132-133: "working tree must be committed and clean before any state-changing stage transition, not only at the stages whose exit conditions restate it") and added "tree is clean" to Stage 3 exit (L271) and Stage 4 exit (L285). Now covered universally + all six stages + success_criteria L403. ADR-0002 compliant prose; no deterministic contract moved. | commit 1ed34d2 |
| 008 | inv8-stop-on-skew-or-partial-install | closed | low | P-none. INV8 present: skill `<pre_route_gates>` L139-164 (both gates) + L141-142 ("before any Builder, Validator, Proposer, or ship work"). Matches runbook L42-44. | verified-present |
| 009 | inv9-one-visible-action-per-turn | closed | low | P-none. INV9 present: `<orchestration_loop>` step 8 L127-130 ("Execute exactly one visible workflow action") with verbatim enumerated list incl. "advance a stage" + L307-308. "Exactly one" with "advance a stage" entails "never two stages in one turn". Matches runbook L45-47. | verified-present |
| 010 | gate-version-skew | closed | low | P-none. Version-skew gate present: skill `<pre_route_gates>` L144-151 (stop on missing/mismatched without continuation evidence, load ledger-and-helper.md, do not auto-rewrite version field). Matches runbook L114-128. | verified-present |
| 011 | gate-install-presence-sibling-field | closed | low | P-none. Install-presence sibling-field gate (F001) present: skill L153-160 ("Treat this as a sibling-field gate... do not route from those fields alone"). Load-bearing safety claim preserved. Runbook L143-150 uses definitive "does NOT elevate"; skill uses "not necessarily represented" - softer phrasing but the actionable instruction is correct and safe. | verified-present |
| 012 | gate-precedence-before-route-id | closed | low | P-none. Gate precedence present: skill `<orchestration_loop>` step 5 (gates) precedes step 6 (route from route_id); L162 "Both gates must clear before entering the route catalog". Matches runbook precedence (start-every-turn step 4 before step 5; L171-172). | verified-present |
| 013 | blocking-gates-discriminated-union | closed | low | P-none. The discriminated-union shape (kind:"route_id"/kind:"field", BLOCKING_GATE_FIELD_NAMES tuple) is a deterministic contract in lib/route.ts/lib/contract.ts. Correctly NOT inlined into the skill per ADR-0002; skill L106-107 explicitly forbids restating route union/schema. Matches runbook L152-169 (which documents the contract for the read-only baseline). | false-positive: ADR-0002 |
| 014 | gates-refire-on-happy-path | closed | low | P-none (adversarial pass 2). Runbook L109-112: gates re-fire every turn even when route_id is happy-path. Skill carries the orchestration-authority claim: L141 ("re-fire on every turn") + `<orchestration_loop>` step 5 L121 (gates before route step 6) + L101-103 (blocking_gates/sibling fields as inputs) + L158-160 ("do not route from those fields alone"). The classifier-elevation mechanism is a lib/route.ts detail, correctly deferred. | verified-present |
| 015 | inv5-ce-code-review-personas-stage5 | closed | low | P-none (adversarial pass 2). Runbook L34-36 parenthetical: /ce-code-review personas decide at Stage 5. Skill keeps the invariant (reviewers own findings, orchestrator records: L297-299, L310) and routes Stage 5 to stage-5-final-review.md + findings-and-validators.md where persona mechanics live. Persona naming is a rare-explanation reference detail. | false-positive: ADR-0002 |
| 016 | inv8-gate-scope-superset | closed | low | P-none (adversarial pass 2). Runbook INV8 (L42-44) lists "Builder, Validator, or ship"; version-skew body (L126) adds Proposer. Skill L141-142 uses the SUPERSET "Builder, Validator, Proposer, or ship work" - at least as strong as both. Not weakened. | verified-present |
| 017 | inv9-builder-commit-vs-attempt | closed | low | P-none (adversarial pass 2). Runbook INV9 enumerates "one Builder commit"; skill step 8 L127-130 says "dispatch one Builder attempt". The load-bearing claim (exactly-one-action) is preserved; "attempt" is the more precise term for a dispatch. Not a weakening. | verified-present |
