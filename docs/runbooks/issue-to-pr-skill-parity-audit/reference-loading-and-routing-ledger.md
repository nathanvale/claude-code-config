# Reference loading and routing - findings ledger

Format and protocol: see [README.md](README.md#ledger-format).

| id | signature | status | risk | summary | resolution |
| --- | --- | --- | --- | --- | --- |
| 001 | stage-3-contract-review-cycle-cap-missing | fixed | high | Runbook lists `contract-review-cycle-cap` (Stage 3 five-cycle Contract Review cap, `blocked_reason: contract-review-cycle-cap`) among the 11 stop-and-ask conditions this seam owns, but the skill omitted it: the Stage 3 shell stop conditions named "open Stage 3 P0/P1" only, and the `<fail_stops>` "Escape hatch or iteration cap" row is bound to Stage 4 batch-loop iteration-cap semantics (accepted-risk/replacement resume), which do not cover a Stage 3 cap that resumes via plan/contract revision. | commit 9ab3d7c |
| 002 | convergence-bookkeeping | closed | low | Convergence bookkeeping. Two consecutive independent clean passes after finding 001's fix landed. Pass 1 (top-down table walk): all 15 reference-loading rows present and resolve on disk one level deep; templates listed action-specific; all 14 route ids carried with matching stage meaning; `lib/route.ts` named as runtime source of truth; no `ROUTE_IDS` tuple or `blocking_gates` union inlined; all 11 outside-catalog stop conditions referenced. Pass 2 (widened-surface exhaustive grep): terminal `shipped` carries no spurious reference; `required_reference_ids` mirroring + drift-is-a-finding carried; load-discipline carried; Stage 3 blocked-route reference coverage carried; in-scope stop-checklist items 2-3 carried. Zero new findings both passes. | two-clean-passes |
