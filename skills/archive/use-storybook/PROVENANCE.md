# Provenance: use-storybook

- Source owner: `claude-code-config`, `skills/use-storybook/`.
- Successor to `skills/storybook/` (v1). Same functionality, tighter prose.
- Local deploy copies may be real repo-local directories when that is the
  desired workflow.
- `experience-sdk/.agents/skills/storybook` is currently a real local copy;
  update to `use-storybook` on next deploy.
