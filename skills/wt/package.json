{
	"name": "wt-scripts",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Bun package for the wt skill. Renders per-repo VS Code workspaces from a branch-keyed registry and delegates worktree CRUD to @side-quest/git, through a facade-backed agent-native CLI.",
	"scripts": {
		"wt": "bun run src/wt.ts",
		"test": "bun test src",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"@side-quest/cli-command-facade": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
