# Provenance: decision-mode

Source: Nathan's own authored skill — `~/.agents/skills/decision-mode/`
License: none (first-party).
Copied: 2026-05-30 into `skills/` (the `~/.claude/skills` symlink target).

This is a COPY, not a symlink. The original lives at `~/.agents/skills/decision-mode/`
(a different tree than the repo's `../../.agents/skills` relative-symlink target at
`~/code/.agents/skills/`). Edits here do not propagate to the original. If you later
add decision-mode to `~/code/.agents/skills/`, this can become a relative symlink
matching grill-me / find-skills.

Self-contained: SKILL.md, REFERENCE.md, EXAMPLES.md cross-reference only each other.
References to `AGENTS.md` / `CONTEXT.md` are decision-owner concepts, not file paths.
