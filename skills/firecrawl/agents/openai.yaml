interface:
  display_name: "Firecrawl"
  short_description: "Run an approved Firecrawl public-web search"
  default_prompt: "Use $firecrawl for this explicitly selected Firecrawl search. Prepare the request and stop for task-local approval before dispatch."
policy:
  allow_implicit_invocation: true
