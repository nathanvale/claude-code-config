# Skill Memory Storage Routing

Moved.

## Owner Path

- Portable storage routing map: `../../context-advisor/references/storage-routing.md`.
- Advisor front door: `../../context-advisor/SKILL.md`.

## Fallback Rule

- Use `context-advisor` when it is installed and invokable.
- If `context-advisor` is unavailable, read `../../context-advisor/references/storage-routing.md` directly.
- If neither the skill nor the reference exists, stop and record a blocked dependency rule in `TASKS.md`.
