{
	"name": "create-cli-scripts",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Local scratch/scripts area for the create-cli skill. Holds the workspace @side-quest/cli-command-facade so generated CommandFacadeContract skeletons can be type-checked and run against the real runtime validator (defineCommandFacadeContract / parseCommandFacadeContract).",
	"bin": {
		"create-cli-facade-smoke": "./facade-resolution-smoke.ts"
	},
	"scripts": {
		"create-cli-facade-smoke": "./facade-resolution-smoke.ts",
		"smoke": "bun run facade-resolution-smoke.ts",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"@side-quest/cli-command-facade": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "*",
		"typescript": "^5.9.3"
	}
}
