{
  "name": "prototype-vocab-map-deps",
  "private": true,
  "description": "THROWAWAY deps for the multi-engine facade spike: the two CLI engines (warm-bin timing) + the workspace cli-command-facade so preflight-warm-chrome resolves.",
  "dependencies": {
    "@playwright/cli": "^0.1.14",
    "chrome-devtools-mcp": "^1.2.0",
    "@side-quest/cli-command-facade": "workspace:*"
  }
}
