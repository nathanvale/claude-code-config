# Personal Product Hot Memory

## Project

- This repo owns local product truth for the product it contains.

## Current Focus

- Keep this file lean.
- Use it for active product context only.
- Move durable detail into `memory/` and full authored material into `docs/`.

## Always / Never

- Keep only the product rules and focus that matter in most sessions.
- Put stable repo policy in `AGENTS.md`.
- Avoid duplicating roadmap, specs, or research here.

## Key Paths

- `AGENTS.md` for durable repo rules
- `TASKS.md` for active work
- `docs/brainstorms/`, `docs/specs/`, and `docs/plans/` for product shaping
- `memory/` for durable compact recall

## Memory OS

- Shared user-scope contract: `~/.config/memory/AGENTS.md`
- Repo profile: `personal-product`
- Use exactly one repo-level hot-memory file.
