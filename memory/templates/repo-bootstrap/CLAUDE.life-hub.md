# My Second Brain Hot Memory

## Current Focus

- Keep this file lean.
- Use it for active operating context only.
- Move durable detail into `memory/` when it stabilizes.
- Put full authored material in `docs/`.
- Use exactly one repo-level hot-memory file.

## Memory OS

- Shared user-scope contract: `~/.config/memory/AGENTS.md`
- Repo profile: `life-hub`
- This repo is for personal control-plane context, promoted durable knowledge, and federated recall entrypoints.
- Storage model: `CLAUDE.md` for hot cache, `memory/` for durable recall, `docs/` for full documents.
- Do not use this file as a dumping ground.

## Migration Notes

- Migrate old material in curated batches.
- Prefer active projects, people, and durable context first.
- Leave archives and low-signal history for later.
- Do not create new PARA storage folders in this repo.
